services:
  coder:
    # This MUST be stable for our documentation and
    # other automations.
    image: ${CODER_REPO:-ghcr.io/coder/coder}:${CODER_VERSION:-v2.33.8}
    restart: unless-stopped # SRV-03
    ports:
      - "7080:7080"
    environment:
      CODER_PG_CONNECTION_URL: "postgresql://${POSTGRES_USER:-username}:${POSTGRES_PASSWORD:-password}@database/${POSTGRES_DB:-coder}?sslmode=disable"
      CODER_HTTP_ADDRESS: "0.0.0.0:7080"
      # You'll need to set CODER_ACCESS_URL to an IP or domain
      # that workspaces can reach. This cannot be localhost
      # or 127.0.0.1 for non-Docker templates!
      CODER_ACCESS_URL: "${CODER_ACCESS_URL:-http://127.0.0.1:7080}" # CFG-03
      CODER_WILDCARD_ACCESS_URL: "${CODER_WILDCARD_ACCESS_URL:-}" # CFG-04; empty = wildcard apps disabled (quickstart)
      # Google login via OIDC (opt-in). Coder only ENABLES OIDC when
      # CODER_OIDC_CLIENT_ID is non-empty, so these defaults leave sign-in
      # off and the zero-config quickstart unaffected. Drop real credentials
      # into .env to activate Google sign-in (requires a reachable HTTPS
      # CODER_ACCESS_URL — Google's redirect callback can't be 127.0.0.1).
      CODER_OIDC_ISSUER_URL: "${CODER_OIDC_ISSUER_URL:-https://accounts.google.com}"
      CODER_OIDC_CLIENT_ID: "${CODER_OIDC_CLIENT_ID:-}"          # empty = OIDC disabled
      CODER_OIDC_CLIENT_SECRET: "${CODER_OIDC_CLIENT_SECRET:-}"  # sensitive; set in .env
      CODER_OIDC_SCOPES: "${CODER_OIDC_SCOPES:-openid,profile,email}"
      # Comma-separated allowed email domains. Restricts logins on the Coder
      # side regardless of who owns the Google project; empty = allow any.
      CODER_OIDC_EMAIL_DOMAIN: "${CODER_OIDC_EMAIL_DOMAIN:-basis.cloud}"
    healthcheck: # SRV-04; start_period covers Coder's DB-migration window
      test: ["CMD", "curl", "-f", "http://localhost:7080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    # The coder user needs write access to the mounted Docker socket (below) to
    # provision workspaces. Set DOCKER_GROUP_GID in .env to the socket's group ID
    # (keeps the setting out of this tracked file, so git pull never reverts it):
    #   - Docker Desktop (macOS/Windows): the socket is root-owned inside the
    #     container, so GID 0 (the default) is correct — nothing to set.
    #   - Linux: set DOCKER_GROUP_GID to the host's docker group GID. Find it
    #     with `stat -c '%g' /var/run/docker.sock` (commonly 999 or 998), then
    #     `docker compose up -d --force-recreate coder` to apply.
    group_add:
      - "${DOCKER_GROUP_GID:-0}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      # Run "docker volume rm coder_coder_home" to reset the dev tunnel url (https://abc.xyz.try.coder.app).
      # This volume is not required in a production environment - you may safely remove it.
      # Coder can recreate all the files it needs on restart.
      - coder_home:/home/coder
      # BBj build context: operator-supplied BBj*.jar + certificate.bls + template Dockerfile +
      # playback.properties. Bind-mounted read-only so the coder provisioner can build the
      # bbj-services workspace image. Contents are license-gated and must NOT be committed to git.
      - ${BBJ_ASSETS_PATH:-./bbj-assets}:/mnt/bbj-assets:ro
    depends_on:
      database:
        condition: service_healthy
  database:
    # Minimum supported version is 13.
    # More versions here: https://hub.docker.com/_/postgres
    image: "postgres:17"
    restart: unless-stopped # SRV-03
    # Uncomment the next two lines to allow connections to the database from outside the server.
    #ports:
    #  - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-username} # The PostgreSQL user (useful to connect to the database)
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password} # The PostgreSQL password (useful to connect to the database)
      POSTGRES_DB: ${POSTGRES_DB:-coder} # The PostgreSQL default database (automatically created at first launch)
    volumes:
      - ${CODER_PG_DATA_DIR:-coder_pgdata}:/var/lib/postgresql/data # SRV-01; default named volume (cross-platform, no chown). Set CODER_PG_DATA_DIR=./data/postgres for a host bind mount (Linux; requires chown 999:999 — see README/OPS-03)
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-username} -d ${POSTGRES_DB:-coder}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 30s   # cover initdb on first boot (WR-05)
volumes:
  coder_home:
  coder_pgdata:
  # NOTE: coder_data removed. Postgres data defaults to the coder_pgdata named volume (SRV-01, cross-platform).
  # Set CODER_PG_DATA_DIR=./data/postgres to opt into a host bind mount (Linux; requires chown 999:999 — see README/OPS-03).
