---
phase: 01-compose-hardening-configuration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .env.example
  - README.md
autonomous: false
requirements: [CFG-01, OPS-01, OPS-02, OPS-03]
must_haves:
  truths:
    - "A new contributor can clone, run `cp .env.example .env`, fill in values, and bring up the stack — every required variable in compose.yaml is documented in .env.example with a safe placeholder (CFG-01)"
    - "README documents the external reverse-proxy contract precisely enough to configure any proxy: :7080 upstream, wildcard TLS, verbatim Host header, WebSocket Upgrade/Connection forwarding, X-Forwarded-* headers, response buffering off, DERP Upgrade passthrough (OPS-01, D-05, D-06)"
    - "README documents first-admin bootstrap: bring up → wait healthy → open CODER_ACCESS_URL → complete UI first-run; no manual start-order dance needed because depends_on enforces DB-first (OPS-02, D-01, D-02)"
    - "README makes the `sudo chown -R 999:999 ./data/postgres` prerequisite prominent BEFORE the `docker compose up` step and states the failure symptom (DB won't initialize) (OPS-03, D-03, D-04)"
  artifacts:
    - path: ".env.example"
      provides: "Committed reference documenting every compose variable with safe placeholders"
      contains: "CODER_ACCESS_URL"
    - path: "README.md"
      provides: "Operator runbook: bring-up sequence, chown prerequisite, first-admin bootstrap, reverse-proxy contract, upgrade-from-quickstart note"
      contains: "chown -R 999:999"
  key_links:
    - from: ".env.example variable set"
      to: "compose.yaml ${VAR} references"
      via: "1:1 variable coverage"
      pattern: "CODER_ACCESS_URL|CODER_WILDCARD_ACCESS_URL|CODER_VERSION|CODER_PG_DATA_DIR|POSTGRES_"
    - from: "README bring-up sequence"
      to: "compose.yaml + .env.example"
      via: "documented clone→cp→chown→up→open-URL flow"
      pattern: "docker compose up"
---

## Phase Goal

**As an** operator, **I want to** follow a clear runbook and a complete `.env.example` to configure and stand up the hardened stack behind my own reverse proxy, **so that** I (or a new contributor) can deploy a trustworthy Coder server at a real public URL without prior Coder knowledge.

<objective>
Slice 2 (builds on the Plan 01 skeleton): deliver the configuration contract (`.env.example`) and the operator runbook (`README.md`) that make the hardened stack reproducible and trustworthy. Plan 01 produced a stack that comes up; this plan makes a stranger able to bring it up correctly — every variable documented, the chown prerequisite impossible to miss, first-admin bootstrap and the reverse-proxy contract spelled out.

Purpose: Close the human-facing requirements (CFG-01, OPS-01..03) so success criteria 3 and 4 (new-contributor onboarding, proxy + bootstrap docs) are met. The `.env`/`.env.example` contract is also the configuration source Phases 2 (backup scripts) and 3 (workspace template) read from.
Output: New `.env.example`; new `README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-compose-hardening-configuration/01-CONTEXT.md
@.planning/phases/01-compose-hardening-configuration/01-RESEARCH.md
@.planning/phases/01-compose-hardening-configuration/01-PATTERNS.md
@.planning/phases/01-compose-hardening-configuration/01-VALIDATION.md
@.planning/phases/01-compose-hardening-configuration/01-SKELETON.md
@.planning/phases/01-compose-hardening-configuration/01-01-SUMMARY.md
@CLAUDE.md
@compose.yaml
</context>

## Artifacts this phase produces

This plan creates:
- **`.env.example`** (new, committed) — documents every variable referenced in `compose.yaml`: `CODER_ACCESS_URL` (placeholder `https://coder.example.com`), `CODER_WILDCARD_ACCESS_URL` (placeholder `*.coder.example.com`), `CODER_VERSION` (`v2.33.8`), optional `CODER_REPO`, `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` (placeholder password `change-me-in-production`), `CODER_PG_DATA_DIR` (`./data/postgres`). Excludes `CODER_TELEMETRY_ENABLE` (D-10) and `CODER_FIRST_USER_*` (D-01).
- **`README.md`** (new) — operator runbook with: prerequisites, bring-up sequence, prominent chown prerequisite (OPS-03), first-admin bootstrap (OPS-02), reverse-proxy contract (OPS-01), upgrade-from-quickstart named-volume migration note, common operations, `coder_home` safe-to-remove note (D-09).

(`compose.yaml` and `.gitignore` are produced by Plan 01 — do NOT modify them here.)

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example — full variable reference with safe placeholders</name>
  <files>.env.example</files>
  <read_first>
    - compose.yaml (the hardened file from Plan 01 — the source of truth for which ${VAR} references must be documented)
    - .planning/phases/01-compose-hardening-configuration/01-RESEARCH.md (Pattern 5 layout, variable behaviors, A1/A2 fallback forms)
    - .planning/phases/01-compose-hardening-configuration/01-PATTERNS.md (.env.example constraints: grouped headers, fake password, full-URL access placeholder, subdomain wildcard, exclusions)
    - .planning/phases/01-compose-hardening-configuration/01-CONTEXT.md (D-08 wildcard cookie-scope warning; D-10 telemetry excluded; D-01 no first-user vars)
  </read_first>
  <action>
    Create `.env.example` at the repo root (CFG-01). Document EVERY variable that compose.yaml interpolates, grouped under `# == Section ==` comment headers, each variable preceded by a short comment explaining purpose and safe default.

    Coder Server section:
    - `CODER_ACCESS_URL=https://coder.example.com` — comment: real public URL operators/workspaces use; setting a real URL disables the *.try.coder.app dev tunnel (CFG-03); must be reachable from workspace containers (127.0.0.1 only works for Docker-local templates).
    - `CODER_WILDCARD_ACCESS_URL=*.coder.example.com` — comment: wildcard subdomain for workspace app URLs (CFG-04); per D-08 do NOT use a top-level domain (e.g. `*.workspaces`) — browsers reject cookies from TLDs and break workspace apps.
    - `CODER_VERSION=v2.33.8` — comment: pinned stable image tag; change only when intentionally upgrading. Include an optional commented `# CODER_REPO=ghcr.io/coder/coder` line documenting the registry override.

    PostgreSQL section:
    - `POSTGRES_USER=coder`
    - `POSTGRES_PASSWORD=change-me-in-production` — obviously-fake placeholder (must be changed before production).
    - `POSTGRES_DB=coder`
    - `CODER_PG_DATA_DIR=./data/postgres` — comment: host bind-mount path for Postgres data; pre-create it and run `sudo chown -R 999:999 $CODER_PG_DATA_DIR` before the first `docker compose up` (cross-reference README/OPS-03).

    Do NOT include `CODER_TELEMETRY_ENABLE` (D-10: leave at Coder's default). Do NOT include any `CODER_FIRST_USER_*` variable (D-01: no admin password in env). Use no real secrets anywhere.
  </action>
  <verify>
    <automated>cd /workspaces/coder && for v in CODER_ACCESS_URL CODER_WILDCARD_ACCESS_URL CODER_VERSION POSTGRES_USER POSTGRES_PASSWORD POSTGRES_DB CODER_PG_DATA_DIR; do grep -q "^$v=" .env.example || { echo "MISSING $v"; exit 1; }; done; ! grep -Eq 'CODER_TELEMETRY_ENABLE|CODER_FIRST_USER' .env.example && ! git check-ignore .env.example >/dev/null && echo "ENV_EXAMPLE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - All seven required variables present at line-start: `grep -q "^VAR="` succeeds for CODER_ACCESS_URL, CODER_WILDCARD_ACCESS_URL, CODER_VERSION, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, CODER_PG_DATA_DIR (CFG-01 — every compose ${VAR} documented).
    - `grep -Ev '^[[:space:]]*#' .env.example | grep -c '^[A-Z]'` equals 7 (no undocumented extras, no leakage of excluded vars).
    - `grep -E 'CODER_TELEMETRY_ENABLE|CODER_FIRST_USER' .env.example` returns nothing (D-10, D-01 exclusions).
    - `grep CODER_WILDCARD_ACCESS_URL .env.example` shows a subdomain placeholder (`*.coder.example.com`), not a TLD (D-08).
    - `POSTGRES_PASSWORD` value is the obviously-fake `change-me-in-production` (no real secret).
    - `git check-ignore .env.example` exits non-zero (the example stays trackable).
  </acceptance_criteria>
  <done>.env.example documents all seven compose-referenced variables with safe placeholders and grouped comments, excludes telemetry and first-user vars, uses a subdomain wildcard placeholder, and is not gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Write README.md operator runbook (bring-up, chown, bootstrap, proxy contract)</name>
  <files>README.md</files>
  <read_first>
    - compose.yaml (hardened stack from Plan 01 — commands and behavior the README documents)
    - .env.example (created in Task 1 — the variables the bring-up sequence references)
    - .planning/phases/01-compose-hardening-configuration/01-PATTERNS.md (README.md content requirements: 8 ordered sections, exact bring-up command block)
    - .planning/phases/01-compose-hardening-configuration/01-RESEARCH.md (Pattern 7 reverse-proxy contract table; Pitfall 1 chown symptom; Open Question 3 named-volume migration command; Pattern 4 reboot caveat)
    - .planning/phases/01-compose-hardening-configuration/01-CONTEXT.md (D-01..D-06, D-09 — bootstrap, proxy-prose-only, chown prominence, coder_home note)
    - CLAUDE.md (Commands section; reverse-proxy / access-URL gotchas)
  </read_first>
  <action>
    Create `README.md` at the repo root covering these sections in order, with the chown step impossible to miss (OPS-03, D-04):

    1. **Title + one-paragraph overview** — production-ready Docker Compose scaffold for self-hosting Coder with durable Postgres on a host bind mount; TLS is the external proxy's responsibility (HTTP-only on :7080).
    2. **Prerequisites** — Docker Engine with Compose v2 (`docker compose`, not `docker-compose`); for production, a public domain plus an external reverse proxy that terminates TLS and supports WebSockets.
    3. **Quick start / bring-up sequence** — the exact ordered command block: `git clone` + `cd`; `cp .env.example .env` then edit real values (CODER_ACCESS_URL, POSTGRES_PASSWORD, etc.); `mkdir -p ./data/postgres`; `sudo chown -R 999:999 ./data/postgres`; `docker compose up -d`; `docker compose ps` (wait until both show `(healthy)`); open `CODER_ACCESS_URL` and complete first-run admin creation. The chown line MUST appear in this block BEFORE the `docker compose up` line (OPS-03).
    4. **Postgres bind-mount ownership (CRITICAL)** — a dedicated, prominent subsection (not a footnote) explaining the `sudo chown -R 999:999 $CODER_PG_DATA_DIR` requirement and stating the exact failure symptom if skipped: the `database` service exits immediately and `docker compose logs database` shows `chown: changing ownership of '/var/lib/postgresql/data': Permission denied`, so `coder` never starts (D-03, D-04, OPS-03).
    5. **First-admin bootstrap** — open `CODER_ACCESS_URL` in a browser and complete the built-in UI first-run to create the initial admin (D-01: no env-var autocreate, no admin password in .env). Note that no manual start-order dance is needed because `depends_on: condition: service_healthy` enforces DB-first ordering on `docker compose up` (D-02). State the reboot caveat from RESEARCH Pattern 4 (on host reboot Docker may start containers concurrently; Coder retries the DB connection until Postgres is ready).
    6. **Reverse-proxy contract** — prose only, proxy-agnostic, no bundled config files (D-05). List the exact requirements (D-06, OPS-01): upstream `http://127.0.0.1:7080` (or host IP if the proxy is off-host); proxy terminates TLS (Coder speaks HTTP only); a wildcard TLS certificate for `*.<apps-domain>` matching `CODER_WILDCARD_ACCESS_URL`; forward the original `Host` header verbatim; forward WebSocket `Upgrade`/`Connection` upgrade headers (terminal I/O); do not strip the DERP `Upgrade` value; disable response buffering (terminal/log streaming); set `X-Forwarded-For` and `X-Forwarded-Proto: https`. State the hard requirement: any proxy in front of Coder MUST support WebSockets.
    7. **Upgrading from the quickstart** — note that switching the old `coder_data` named volume to the bind mount loses data unless migrated; give the one-shot migration command from RESEARCH Open Question 3 (`docker run --rm -v coder_coder_data:/data -v $(pwd)/data/postgres:/target alpine sh -c 'cp -a /data/. /target/'`), then fix ownership with chown, then `docker compose up -d`. Fresh installs need no migration.
    8. **Common operations + coder_home note** — `docker compose logs -f coder`, `docker compose pull && docker compose up -d` (upgrade), and the volume-reset commands. State that the `coder_home` named volume is a dev-tunnel convenience and is safe to remove in production (D-09).

    Use `docker compose` (v2) everywhere, never `docker-compose`. Do not invent a proxy example config.
  </action>
  <verify>
    <automated>cd /workspaces/coder && grep -q 'chown -R 999:999' README.md && awk '/chown -R 999:999/{c=NR} /docker compose up/{u=NR} END{exit !(c>0 && u>0 && c<u)}' README.md && grep -Eqi 'websocket|Upgrade' README.md && grep -q 'CODER_WILDCARD_ACCESS_URL' README.md && grep -q 'CODER_ACCESS_URL' README.md && ! grep -q 'docker-compose ' README.md && echo "README_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'chown -R 999:999' README.md` matches and the chown line appears BEFORE the first `docker compose up` line (OPS-03, D-04 — verified by the awk line-order check).
    - README states the chown failure symptom: `grep -i "Permission denied" README.md` matches (operator can self-diagnose).
    - Reverse-proxy contract present and complete: `grep -Ei 'websocket|Upgrade'`, `grep Host README.md`, `grep -i 'X-Forwarded'`, `grep -i 'buffer'`, and `grep -i 'wildcard'` all match (OPS-01, D-06 9-point checklist).
    - First-admin bootstrap documented: `grep -Ei 'first-run|first admin|admin' README.md` matches and references opening `CODER_ACCESS_URL` (OPS-02, D-01).
    - `grep -i 'depends_on\|service_healthy\|DB-first\|database.*first' README.md` matches (D-02 ordering explained).
    - No `docker-compose ` (v1 hyphenated) usage: `grep 'docker-compose ' README.md` returns nothing.
    - `grep -i 'coder_home' README.md` matches with safe-to-remove note (D-09).
  </acceptance_criteria>
  <done>README.md is a complete operator runbook: the chown prerequisite is prominent and precedes `docker compose up` with its failure symptom stated, first-admin bootstrap and DB-first ordering are documented, and the 9-point reverse-proxy contract plus upgrade and coder_home notes are present — all using `docker compose` v2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>.env.example documenting every compose variable with safe placeholders, and README.md — the operator runbook covering bring-up, the prominent chown prerequisite, first-admin bootstrap, and the reverse-proxy contract. This checkpoint confirms the prose is correct and complete by a human, which automated greps cannot fully judge (OPS-01..03 are manual-review requirements per 01-VALIDATION.md).</what-built>
  <how-to-verify>
    1. Open `.env.example` and confirm every variable has a clear comment and a safe (non-secret) placeholder; confirm the wildcard uses a subdomain and the password is obviously fake.
    2. Read README.md top to bottom as if you had never used Coder. Confirm you could clone → `cp .env.example .env` → edit → chown → `docker compose up` → open URL → create admin without external knowledge.
    3. Confirm the chown step is impossible to miss and appears before `docker compose up`, and that the failure symptom is stated.
    4. Check the reverse-proxy section against the 9-point D-06 checklist in 01-VALIDATION.md (`:7080`, wildcard TLS, Host verbatim, WebSocket Upgrade/Connection, DERP Upgrade passthrough, no buffering, X-Forwarded-For, X-Forwarded-Proto, "must support WebSockets").
    5. Optionally run the full smoke sequence end-to-end following only the README: `docker compose down && docker compose up -d && docker compose ps && docker compose exec -T coder curl -fsS http://localhost:7080/healthz` → expect `OK`.
    Report any missing or unclear section.
  </how-to-verify>
  <resume-signal>Type "approved" if .env.example is complete and README lets a stranger stand up the stack with the 9-point proxy contract present — or describe gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo → public/remote | `.env.example` is committed; it must contain only placeholders, never real secrets |
| operator browser/proxy → Coder :7080 | Documentation defines the proxy contract that protects this ingress |
| operator → first-run admin creation | Initial privileged account is created via the UI, not via committed config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | Real secret pasted into committed `.env.example` | mitigate | `.env.example` uses only placeholders (`change-me-in-production`); the obviously-fake password signals it must be replaced (Task 1) |
| T-01-07 | Information Disclosure | Admin password persisted in `.env` | mitigate | D-01: README documents manual UI first-run; no `CODER_FIRST_USER_*` in `.env.example` (Task 1, Task 2) |
| T-01-08 | Tampering / Spoofing | Misconfigured reverse proxy strips Host/WebSocket/DERP | mitigate | README proxy contract mandates verbatim Host, WebSocket and DERP Upgrade passthrough, X-Forwarded-Proto (Task 2, OPS-01) |
| T-01-09 | Information Disclosure | Dev tunnel left active because operator did not set a real access URL | mitigate | README + `.env.example` instruct setting a real `CODER_ACCESS_URL`, which disables the *.try.coder.app tunnel (Task 1, Task 2) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — documentation and a placeholder env file only (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `.env.example`: all seven compose variables documented, telemetry/first-user excluded, subdomain wildcard, fake password, not gitignored (Task 1 acceptance).
- `README.md`: chown precedes `up` with symptom, first-admin + DB-first ordering, 9-point proxy contract, no `docker-compose` v1, coder_home note (Task 2 acceptance).
- Human checkpoint: a stranger can stand up the stack from the README; proxy contract complete against the D-06 checklist.
</verification>

<success_criteria>
- A new contributor can `cp .env.example .env`, fill values, and bring up the stack — every required variable documented (CFG-01).
- README documents the reverse-proxy contract precisely (OPS-01, D-05/D-06), the first-admin bootstrap and DB-first ordering (OPS-02, D-01/D-02), and the prominent chown prerequisite with its failure symptom (OPS-03, D-03/D-04).
</success_criteria>

<output>
Create `.planning/phases/01-compose-hardening-configuration/01-02-SUMMARY.md` when done.
</output>
