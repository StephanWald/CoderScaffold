---
phase: 04-portable-claude-config
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - README.md
autonomous: true
requirements: [CLAUDE-07]
must_haves:
  truths:
    - "The README has a Claude Code operator subsection inside the Workspace Template section"
    - "The subsection walks the operator through the first-run `claude` login"
    - "The subsection states what carries across workspaces: auth, settings, personal skills, user-scoped MCP servers"
    - "The subsection explains empty-volume seeding behavior (first run authenticates, persists to the shared per-owner volume)"
    - "The subsection documents the one-active-workspace-per-owner concurrent-write caveat (no file locking on ~/.claude.json; sequential use safe)"
    - "The subsection documents the manual orphaned-volume cleanup: docker volume rm <name>, discoverable via docker volume ls -f label=coder.purpose=claude-config"
  artifacts:
    - path: "README.md"
      provides: "Claude Code operator runbook subsection covering login, sharing, seeding, concurrent-write caveat, volume cleanup"
      contains: "label=coder.purpose=claude-config"
  key_links:
    - from: "README.md Claude Code subsection"
      to: "templates/docker/main.tf claude_config_volume"
      via: "references the coder-<owner-uuid>-claude volume and its discovery label"
      pattern: "coder.purpose=claude-config"
---

<objective>
Add a focused operator runbook subsection to `README.md` covering portable Claude Code config: first-run login, what is shared across workspaces, empty-volume seeding behavior, the concurrent-write caveat, and manual orphaned-volume cleanup (CLAUDE-07 / D-09, D-10).

Purpose: Operators understand that authenticating once persists Claude auth/settings/skills/MCP across all of an owner's workspaces, why only one workspace per owner should run `claude` at a time, and how to clean up the prevent_destroy volume manually.
Output: A modified `README.md` with a new `### Claude Code` subsection inside `## Workspace Template`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-portable-claude-config/04-CONTEXT.md
@.planning/phases/04-portable-claude-config/04-01-SUMMARY.md
@README.md
</context>

<artifacts_this_phase_produces>
These symbols are CREATED by this phase (source-grounding pass: treat as newly introduced):

- `### Claude Code` subsection in README.md (inside `## Workspace Template`, after `### Home directory persistence`)
- Operator-facing prose + commands: the `claude` first-run login walkthrough, the shared-surface list, the seeding explanation, the concurrent-write caveat, and the `docker volume rm` / `docker volume ls -f label=coder.purpose=claude-config` cleanup commands
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the Claude Code operator runbook subsection to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 294-398 (the `## Workspace Template` section — match its heading style, prose density, `> **Note:**` blockquote callouts, and fenced bash blocks; the new subsection goes after line 398)
    - .planning/phases/04-portable-claude-config/04-PATTERNS.md section 6 (README analog + style signals) and the placement note (after `### Home directory persistence`)
    - .planning/phases/04-portable-claude-config/04-CONTEXT.md D-09 (the exactly-four topics) and D-10 (manual cleanup note)
    - .planning/phases/04-portable-claude-config/04-01-SUMMARY.md (the FINAL volume-name expression and mount path produced by Plan 01 — cite these exactly so the doc matches the shipped template)
  </read_first>
  <action>
    Append a new `### Claude Code` H3 subsection at the end of the `## Workspace Template` section (after line 398, the end of `### Home directory persistence`), still inside that H2. Match the existing README style: one-sentence intro, prose paragraphs, fenced `bash` blocks for commands, and `> **Note:**` blockquotes for caveats. Cover exactly the four D-09 topics plus the D-10 cleanup note:

    1. First-run login walkthrough: open a workspace, run `claude` in a terminal, complete the interactive OAuth/subscription login once. Note that no API key is required by default (the `anthropic_api_key` template variable defaults empty so OAuth is the default path); an operator who prefers API-key auth can set that variable but it is optional.
    2. What carries across workspaces: list auth/credentials, global settings, personal skills, and user-scoped MCP servers (stored under `~/.claude/` and `~/.claude.json`) — all live on a per-owner Docker volume named `coder-<owner-uuid>-claude` (cite the exact expression from the 04-01 SUMMARY) shared by every workspace that owner creates, including newly created ones, with no manual copy step.
    3. Empty-volume seeding behavior: the volume starts empty; the first `claude` login authenticates and writes into it; subsequent workspaces for the same owner start already authenticated. Mention the neutral mount at `/home/coder/.claude-shared` with `~/.claude` and `~/.claude.json` symlinked into it (so the reader understands where data lives).
    4. Concurrent-write caveat (use a `> **Note:**` blockquote): run `claude` in only one active workspace per owner at a time. `~/.claude.json` has no file locking, so concurrent writes from two simultaneously-running workspaces can corrupt session state. Sequential use across workspaces is safe.

    Then add the manual cleanup note (D-10): because the volume is created with `prevent_destroy = true`, deleting a Coder user or workspace never removes it — over time deleted owners leave orphaned `coder-<owner-uuid>-claude` volumes. Provide a fenced bash block showing discovery via `docker volume ls -f label=coder.purpose=claude-config` and removal via `docker volume rm <name>`. State plainly that there is no cleanup script — this is a deliberate documented manual step.

    Optionally add a one-line note that the repo's own `.devcontainer/devcontainer.json` mounts only `~/.claude` and omits `~/.claude.json` (the gap this template's symlink approach closes) — keep it to a single sentence; do not modify the devcontainer. Keep the whole subsection concise, matching the repo's existing README density — no extended troubleshooting appendix.
  </action>
  <verify>
    <automated>grep -q '^### Claude Code' README.md && grep -q 'claude' README.md && grep -q 'label=coder.purpose=claude-config' README.md && grep -q 'docker volume rm' README.md && grep -qi 'concurrent\|one active workspace\|simultaneously' README.md && grep -qi 'MCP' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### Claude Code' README.md` returns 1 and the heading sits inside the `## Workspace Template` section (after `### Home directory persistence`)
    - The subsection mentions running `claude` for first-run login and states no API key is required by default
    - It lists all four shared items: auth/credentials, settings, (personal) skills, and (user-scoped) MCP servers
    - It explains empty-volume seeding and references the per-owner `coder-<owner-uuid>-claude` volume by the exact name expression from the 04-01 SUMMARY
    - A `> **Note:**` (or equivalent) callout documents the concurrent-write caveat (one active workspace per owner; no `~/.claude.json` file locking; sequential safe)
    - The cleanup note includes both `docker volume ls -f label=coder.purpose=claude-config` and `docker volume rm`
  </acceptance_criteria>
  <done>README has a concise `### Claude Code` subsection covering login, the four shared items, seeding, the concurrent-write caveat, and the manual prevent_destroy volume cleanup — all matching the existing README style and the final names from Plan 01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → documented manual cleanup | README instructs running `docker volume rm` on a host volume — a destructive host command |
| reader → concurrent-write guidance | README is the only control mitigating the unfixed `~/.claude.json` concurrent-write race |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04D-01 | Tampering | documented `docker volume rm` cleanup | mitigate | Cleanup command paired with the discovery command (`docker volume ls -f label=coder.purpose=claude-config`) so operators target the correct owner volume by label, reducing the chance of deleting the wrong volume; prose states the volume holds auth/config (deletion forces re-login, not data-unsafe). |
| T-04D-02 | Repudiation / data loss | concurrent multi-workspace writes | accept (documented) | README `> **Note:**` makes the "one active workspace per owner" caveat explicit (no file locking on `~/.claude.json`). Engineering a lock is out of scope (REQUIREMENTS §Out of Scope); documentation is the chosen control. |

No `high`-severity threat introduced by documentation. Both items are mitigated or explicitly accepted with operator-facing guidance.
</threat_model>

<verification>
Automated (authoritative): the grep assertions in Task 1 `<verify>` pass against `README.md`.

Manual review (operator UAT, optional): read the rendered `### Claude Code` subsection and confirm it covers the four SC-5 topics (login, what is shared, seeding, concurrent-write caveat) plus the cleanup note, and that the volume name matches the shipped `templates/docker/main.tf`.
</verification>

<success_criteria>
- README contains a `### Claude Code` operator subsection inside `## Workspace Template` covering: first-run `claude` login, the four shared items (auth, settings, skills, MCP servers), empty-volume seeding, the one-active-workspace-per-owner concurrent-write caveat, and the manual `prevent_destroy` volume cleanup (discovery + removal commands).
- The volume name and discovery label cited in the README match the final identifiers from Plan 01's SUMMARY.
- CLAUDE-07 is satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-portable-claude-config/04-02-SUMMARY.md` when done. Record the heading text added, its placement, and confirm the cited volume name matches Plan 01.
</output>
