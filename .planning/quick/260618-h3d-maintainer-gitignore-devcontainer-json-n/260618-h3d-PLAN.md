---
phase: quick-260618-h3d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .devcontainer/devcontainer.json
  - templates/coderscaffold/main.tf
  - templates/coderscaffold/Dockerfile
autonomous: true
requirements: []

must_haves:
  truths:
    - "`.gitignore` ignores Terraform/OS/editor/Coder-CLI noise but does NOT match .devcontainer/devcontainer.json or devcontainer-lock.json"
    - "devcontainer.json is valid JSON, has no `npm install` postCreate, provides Docker + Terraform + shellcheck tooling, keeps the claude-code:1.0 feature and claude-code-config mount"
    - "templates/coderscaffold/ exists with a .tf file so push-templates.sh discovers and pushes it as 'coderscaffold'"
    - "coderscaffold template mirrors templates/docker/main.tf and adds an idempotent, non-fatal git clone of StephanWald/CoderScaffold in startup_script"
  artifacts:
    - path: ".gitignore"
      provides: "Project-appropriate ignore rules"
    - path: ".devcontainer/devcontainer.json"
      provides: "Maintainer dev container definition"
    - path: "templates/coderscaffold/main.tf"
      provides: "CoderScaffold maintainer workspace template"
    - path: "templates/coderscaffold/Dockerfile"
      provides: "Workspace image with Node.js (for GSD) over the overridable base"
  key_links:
    - from: "templates/coderscaffold/main.tf"
      to: "templates/coderscaffold/Dockerfile"
      via: "docker_image build { context = path.module, dockerfile = Dockerfile }"
      pattern: "dockerfile.*=.*\"Dockerfile\""
---

<objective>
Maintain this Coder Production Scaffold repo with three related changes: (1) make the root `.gitignore` appropriate for a Docker Compose + Terraform + bash project with a maintainer devcontainer; (2) fix `.devcontainer/devcontainer.json` so it actually equips a maintainer of THIS repo (Docker CLI, Terraform, shellcheck) instead of a generic Node project; (3) add a new `templates/coderscaffold/` Coder workspace template that mirrors `templates/docker/` and clones the upstream repo on startup.

Purpose: The current devcontainer is a generic javascript-node image with a meaningless `npm install` postCreate (this repo has no npm project), the .gitignore is minimal, and there is only one workspace template. A maintainer needs the right tools and a ready-made workspace for this project.

Output: Updated `.gitignore`, rewritten `.devcontainer/devcontainer.json`, and new `templates/coderscaffold/{main.tf,Dockerfile}`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@templates/docker/main.tf
@templates/docker/Dockerfile
@scripts/push-templates.sh
@.devcontainer/devcontainer.json
@.devcontainer/devcontainer-lock.json
@.gitignore

VERIFICATION REALITY (project memory: "Infra needs a live deploy gate"):
- The `coder` CLI is NOT installed here and no Coder server is reachable.
- `terraform` is NOT installed in this dev environment (confirmed). `shellcheck` is NOT installed. `jq` IS available.
- Therefore ALL verification in this plan is STATIC ONLY. If `terraform` happens to be present at execute time, run `terraform fmt -check` / `terraform validate`; otherwise SKIP those and note the skip.
- LIVE verification (actual `coder templates push`, actual devcontainer rebuild, actual workspace provisioning) MUST be DEFERRED and documented in the SUMMARY using the same "LIVE VERIFICATION DEFERRED" style already in scripts/push-templates.sh. Do NOT claim the template provisions or the devcontainer builds correctly.

TRACKING NOTE: `.devcontainer/devcontainer.json` and `devcontainer-lock.json` are currently UNTRACKED (git ls-files shows neither). Do NOT change their tracking status in this task and do NOT add a blanket `.devcontainer/` ignore. The new `.gitignore` must NOT match them (verify with `git check-ignore`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore and rewrite devcontainer.json for THIS repo</name>
  <files>.gitignore, .devcontainer/devcontainer.json</files>
  <action>
Two maintainer-environment edits.

(A) `.gitignore` — keep the existing three entries (`.env`, `data/`, `backups/`) and add project-appropriate sections with clarifying comments, matching the existing comment style:
- Terraform noise that can appear when templates are validated/applied locally: `.terraform/`, `*.tfstate`, `*.tfstate.*`, `crash.log`, `crash.*.log`, `*.tfvars` and `*.tfvars.json` EXCEPT keep `*.tfvars.example` if any exist (use a `!` negation only if such a file exists — otherwise omit the negation). Add `override.tf`, `override.tf.json`, `*_override.tf`, `*_override.tf.json`. Leave `.terraform.lock.hcl` a discussable judgment call: do NOT ignore it (committing provider lock files aids reproducibility — consistent with the repo's pin-everything ethos); add a comment line explaining the choice so it is intentional, not accidental.
- OS cruft: `.DS_Store`, `Thumbs.db`.
- Editor cruft: `*.swp`, `*.swo`, `.idea/`, `*.code-workspace`. Do NOT ignore `.vscode/` wholesale (a repo may want to commit shared settings); only ignore editor scratch files.
- Local Coder CLI artifacts: `.coderv2/` (the coder CLI's local config/session dir).
CRITICAL: Do NOT add `.devcontainer/` or any pattern that matches `.devcontainer/devcontainer.json` or `.devcontainer/devcontainer-lock.json` — these are kept TRACKED to pin the claude-code feature. Do NOT change `.planning/` or `.claude/` ignore status.

(B) `.devcontainer/devcontainer.json` — rewrite so it equips a maintainer of THIS repo (Docker Compose + Terraform templates + bash scripts), not a generic Node app:
- `name`: "CoderScaffold Maintainer".
- Keep `image`: `mcr.microsoft.com/devcontainers/javascript-node:1-18-bullseye` (it provides node/npm which GSD/Claude tooling can use; no need to introduce a new base for a maintainer box).
- `features` block — KEEP the existing `ghcr.io/anthropics/devcontainer-features/claude-code:1.0` reference EXACTLY (do not bump it; it must stay consistent with devcontainer-lock.json which pins 1.0.5). ADD:
  - `ghcr.io/devcontainers/features/docker-outside-of-docker:1` (gives a Docker CLI that talks to the host daemon so `docker compose` works; preferred over docker-in-docker for a maintainer box that drives compose on the host).
  - `ghcr.io/devcontainers/features/terraform:1` (Terraform/OpenTofu + tooling to `terraform fmt`/`validate` the templates).
  NOTE in a JSON `//` comment above the features block that these feature MAJOR pins (`:1`) are best-effort and their resolved versions may need LIVE verification on first rebuild (the lock only covers claude-code today).
- shellcheck: the `terraform` feature does not include it. Install shellcheck via `postCreateCommand` instead of a separate feature (simpler, no extra registry dependency): replace the `npm install` postCreate with a non-fatal apt install, e.g. `sudo apt-get update && sudo apt-get install -y shellcheck || echo 'WARN: shellcheck install failed; continuing'`. Do NOT leave `npm install` (there is no npm project — package-lock.json is an 84-byte stub).
- KEEP the existing mount EXACTLY: `source=claude-code-config,target=/home/node/.claude,type=volume`.
- Ensure the file is strict valid JSON when comments are stripped (devcontainer.json is JSONC; `//` line comments are allowed by the spec but `jq` cannot parse them — see verify).
  </action>
  <verify>
    <automated>cd /workspaces/coder && git check-ignore .devcontainer/devcontainer.json .devcontainer/devcontainer-lock.json; test $? -ne 0 && echo "PASS: devcontainer files NOT ignored"</automated>
    <automated>cd /workspaces/coder && git check-ignore -q foo.tfstate .terraform/x .DS_Store .coderv2/config && echo "PASS: tf/os/coder noise IS ignored"</automated>
    <automated>cd /workspaces/coder && ! git check-ignore -q .terraform.lock.hcl && echo "PASS: lock file NOT ignored (intentional)"</automated>
    <automated>cd /workspaces/coder && sed 's://.*$::' .devcontainer/devcontainer.json | jq -e '.name=="CoderScaffold Maintainer" and (.postCreateCommand|test("npm install")|not) and (.features|has("ghcr.io/anthropics/devcontainer-features/claude-code:1.0")) and (.mounts[0]|test("claude-code-config"))' && echo "PASS: devcontainer.json shape valid"</automated>
  </verify>
  <done>`.gitignore` ignores tf/OS/editor/coder noise, keeps `.terraform.lock.hcl` tracked, and does NOT match the two devcontainer files. devcontainer.json is valid JSONC named "CoderScaffold Maintainer", has docker-outside-of-docker + terraform features added, keeps the claude-code:1.0 feature and claude-code-config mount, installs shellcheck via postCreate, and has no `npm install`.</done>
</task>

<task type="auto">
  <name>Task 2: Add templates/coderscaffold/ template mirroring templates/docker/ + repo clone</name>
  <files>templates/coderscaffold/main.tf, templates/coderscaffold/Dockerfile</files>
  <action>
Create a new workspace template in its OWN directory so `scripts/push-templates.sh` discovers it (the loop pushes each `templates/*/` subdir that contains a `.tf` file, using the directory basename as the template name — so the dir MUST be named `coderscaffold` to be pushed as "coderscaffold"). Each template dir is pushed independently and must carry its own Dockerfile, so this template gets its own copy.

(A) `templates/coderscaffold/Dockerfile` — mirror `templates/docker/Dockerfile` VERBATIM in structure: `ARG BASE_IMAGE=codercom/enterprise-base:ubuntu`, `FROM ${BASE_IMAGE}`, install Node.js LTS via NodeSource as root (needed for the GSD `npx` install), then `USER coder`. Update the leading path comment to `templates/coderscaffold/Dockerfile`. Add a one-line comment noting `git` is required by the startup_script clone step and is already present in `codercom/enterprise-base:ubuntu` (so no extra apt install is needed) — keep the comment honest as a confirmation, do not add a redundant `apt-get install git`.

(B) `templates/coderscaffold/main.tf` — copy `templates/docker/main.tf` and keep it structurally identical, preserving ALL of: the `terraform` required_providers block (`coder ~> 2.18`, `kreuzwerker/docker ~> 4.4`, `required_version = ">= 1.9"`); the `docker_socket`, `workspace_image`, and `anthropic_api_key` variables; the docker provider; the three coder_* data sources and `locals`; the `coder_agent.main` with the full per-owner Claude config symlink dance, GIT_* env, and the three metadata blocks; the `docker_volume.home_volume` keyed on workspace UUID with `lifecycle { ignore_changes = [name] }` and labels; the unmanaged per-owner claude volume comment block; `docker_image.main` (build context = path.module, Dockerfile, BASE_IMAGE arg, filesha1 trigger, keep_locally); `docker_container.workspace` with `count = start_count`, the entrypoint host.docker.internal replace, the host gateway entry, BOTH volume mounts (/home/coder then /home/coder/.claude-shared), and labels; and the `code-server` (1.5.0), `jetbrains-gateway` (1.2.6, IUs, default IU), and `claude-code` (5.2.0, install_claude_code=true) modules each with `count = start_count`.

DISTINGUISHING CHANGES (only these differ from templates/docker/main.tf):
- Update the leading header comment block to describe this as the CoderScaffold maintainer template and mention the repo-clone behavior.
- In `coder_agent.main.startup_script`, ADD an idempotent, non-fatal git clone of the upstream repo. Place it AFTER the GSD install-once block. It MUST follow the same WR-03 non-fatal pattern used for GSD: clone into `$HOME/CoderScaffold` ONLY if that directory does not already exist; guard on `command -v git`; never abort the startup_script under `set -e`. Pattern (directive, not literal):
  - `if [ ! -d "$HOME/CoderScaffold" ]; then` then `if command -v git >/dev/null 2>&1; then git clone https://github.com/StephanWald/CoderScaffold.git "$HOME/CoderScaffold" || echo "WARN: CoderScaffold clone failed; continuing" >&2` `else echo "WARN: git not found; skipping CoderScaffold clone" >&2` `fi` `fi`.
  - Add a short comment above it explaining idempotency + non-fatal rationale, matching the GSD block's comment style.
- Point the editor `folder` at the cloned repo for both editors: set `code-server` `folder = "/home/coder/CoderScaffold"` and `jetbrains-gateway` `folder = "/home/coder/CoderScaffold"` (absolute path — relative paths fail jetbrains module validation per the existing Pitfall 4 comment). Keep a brief comment that this points at the cloned upstream repo. (The directory is created by the startup_script clone; the editors open it once the agent has run.)
Do NOT introduce v1/simplified variants, do NOT change module versions, do NOT change provider pins.
  </action>
  <verify>
    <automated>cd /workspaces/coder && test -f templates/coderscaffold/main.tf && test -f templates/coderscaffold/Dockerfile && echo "PASS: files exist"</automated>
    <automated>cd /workspaces/coder && grep -q 'CoderScaffold' templates/coderscaffold/main.tf && grep -Eq 'git clone .*StephanWald/CoderScaffold' templates/coderscaffold/main.tf && grep -q 'command -v git' templates/coderscaffold/main.tf && echo "PASS: idempotent non-fatal clone present"</automated>
    <automated>cd /workspaces/coder && grep -q 'folder *= *"/home/coder/CoderScaffold"' templates/coderscaffold/main.tf && echo "PASS: editors point at cloned repo"</automated>
    <automated>cd /workspaces/coder && grep -q 'version = "~> 2.18"' templates/coderscaffold/main.tf && grep -q 'version = "~> 4.4"' templates/coderscaffold/main.tf && grep -q 'version *= *"5.2.0"' templates/coderscaffold/main.tf && grep -q 'version *= *"1.5.0"' templates/coderscaffold/main.tf && grep -q 'version *= *"1.2.6"' templates/coderscaffold/main.tf && echo "PASS: pins preserved"</automated>
    <automated>cd /workspaces/coder && if command -v terraform >/dev/null 2>&1; then terraform -chdir=templates/coderscaffold fmt -check && terraform -chdir=templates/coderscaffold validate 2>/dev/null || echo "NOTE: validate may need provider init; fmt checked"; else echo "SKIP: terraform not installed — fmt/validate DEFERRED to live env"; fi</automated>
  </verify>
  <done>`templates/coderscaffold/` contains main.tf + Dockerfile. main.tf is structurally identical to templates/docker/main.tf with provider/module pins intact, plus an idempotent non-fatal `git clone https://github.com/StephanWald/CoderScaffold` in startup_script (after the GSD block) and both editors' `folder` pointed at `/home/coder/CoderScaffold`. Dockerfile mirrors templates/docker/Dockerfile (Node.js layer) with a confirmation comment that git ships in the base image. push-templates.sh would discover it as "coderscaffold".</done>
</task>

</tasks>

<verification>
- `git check-ignore` confirms the two `.devcontainer/*.json` files are NOT ignored and tf/OS/coder noise IS ignored.
- `jq` (after stripping `//` comments) confirms devcontainer.json shape.
- `grep` confirms the coderscaffold template preserves all pins and adds the clone + editor folder changes.
- terraform fmt/validate run ONLY if terraform is present; otherwise explicitly skipped and noted.
- ALL live checks (devcontainer rebuild, `coder templates push`, workspace provisioning, actual clone-on-start) are DEFERRED — the SUMMARY must carry a "LIVE VERIFICATION DEFERRED" note in the style of scripts/push-templates.sh.
</verification>

<success_criteria>
- `.gitignore` is project-appropriate, keeps `.terraform.lock.hcl` tracked, and never matches the devcontainer JSON files.
- `.devcontainer/devcontainer.json` is valid JSONC named "CoderScaffold Maintainer" with docker-outside-of-docker + terraform features, claude-code:1.0 kept, shellcheck via postCreate, no `npm install`, claude-code-config mount kept.
- `templates/coderscaffold/{main.tf,Dockerfile}` mirror templates/docker/ exactly except for the documented distinguishing changes (header comment, idempotent non-fatal repo clone, editor folder).
- No claim of successful live deploy; live verification explicitly deferred and documented.
</success_criteria>

<output>
Create `.planning/quick/260618-h3d-maintainer-gitignore-devcontainer-json-n/260618-h3d-SUMMARY.md` when done. Include a "LIVE VERIFICATION DEFERRED" section listing what could not be verified statically (devcontainer rebuild, feature version resolution, `coder templates push`, workspace provisioning, on-start git clone) and which static checks passed (including whether terraform was present).
</output>
