---
phase: quick-260716-9pg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [templates/bbj-services/main.tf]
autonomous: true
requirements: [QUICK-9pg]

must_haves:
  truths:
    - "coder_agent.main.startup_script no longer launches BBjServices in the background"
    - "startup_script ends with a NOTE comment explaining why BBjServices moved to its own coder_script"
    - "A coder_script.bbjservices resource runs BBjServices in the foreground via exec, guarded only by the :8888 port check"
    - "terraform fmt reports no changes"
  artifacts:
    - path: "templates/bbj-services/main.tf"
      provides: "coder_script.bbjservices resource + NOTE comment replacing the old background launch block"
      contains: "resource \"coder_script\" \"bbjservices\""
  key_links:
    - from: "coder_script.bbjservices"
      to: "coder_agent.main"
      via: "agent_id = coder_agent.main.id"
      pattern: "agent_id\\s*=\\s*coder_agent.main.id"
---

<objective>
Convert the BBjServices background launch in `templates/bbj-services/main.tf` from an inline `startup_script` block into a dedicated `coder_script.bbjservices` resource, mirroring the already-proven implementation in the private `bbj-dev` template.

Purpose: A process backgrounded from `startup_script` inherits the script's output pipes; Coder warns ("output pipes were not closed after 10s") and may terminate the child. Running BBjServices as its own `coder_script` (foreground `exec`) fixes this and gives it a per-script dashboard log.
Output: Edited `templates/bbj-services/main.tf` — old background block removed, NOTE comment added, new `coder_script.bbjservices` resource inserted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<!-- Target file: templates/bbj-services/main.tf -->
<!-- Reference (mirror EXACTLY, comments included): /Users/beff/coder-bbj-private/templates/bbj-dev/main.tf -->
<!--   - startup_script NOTE comment: bbj-dev lines 298-303 (adapt: bbj-services has NO svn checkout, so drop the svn wording — NOTE is about the BBjServices launch only) -->
<!--   - coder_script.bbjservices resource: bbj-dev lines 397-424 (copy verbatim, comment header included) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move BBjServices launch into a dedicated coder_script resource</name>
  <files>templates/bbj-services/main.tf</files>
  <action>
Make three edits to `templates/bbj-services/main.tf`, all inside/around `coder_agent.main`:

1. DELETE the entire "── BBjServices — background launch (WR-03 non-fatal)" region from the `startup_script` heredoc. This is the block currently spanning from the leading comment `# ── BBjServices — background launch (WR-03 non-fatal) ────` through the closing `fi` of the `if [ -x /opt/bbx/bin/bbjservices ]; then ... else ... fi` block (the block containing `sudo nohup setsid /opt/bbx/bin/bbjservices --launchd`, the `/tmp/bbjservices.log` redirect, the `/tmp/bbjservices.pid` writes, and the final `WARN: /opt/bbx/bin/bbjservices not found` echo). Remove the whole region including its multi-line commentary about running as root/sudo, the log redirect, idempotency, and non-fatality.

2. REPLACE that deleted region with a short NOTE comment, still inside the `startup_script` heredoc, as the last content before the closing `EOT`. Mirror the NOTE at bbj-dev main.tf lines 298-303 but adapt for bbj-services: bbj-services has NO svn checkout, so the NOTE must reference only the BBjServices launch. Content: a process backgrounded from startup_script inherits the script's output pipes, Coder warns ("output pipes were not closed after 10s") and may terminate the child, so BBjServices runs as its own coder_script resource below (foreground, non-blocking, per-script dashboard log). Keep 4-space indentation matching the surrounding heredoc lines.

3. INSERT a new `resource "coder_script" "bbjservices"` block immediately BEFORE the `resource "docker_volume" "home_volume"` block (and before its `# ── Persistent home volume` comment header). Copy it VERBATIM from bbj-dev main.tf lines 397-424, including the `# ── BBjServices — dedicated script (WR-03 non-fatal) ──` comment header. The resource body: agent_id = coder_agent.main.id, display_name = "BBjServices", run_on_start = true, start_blocks_login = false, and a POSIX-sh `script = <<-EOT` heredoc that (a) if `/opt/bbx/bin/bbjservices` is not executable, WARNs to stderr that BBjServices will not be available on port 8888 and `exit 0`; (b) if `ss -tlnp 2>/dev/null | grep -q ':8888'`, echoes an already-running message and `exit 0`; (c) otherwise echoes a start message and `exec sudo /opt/bbx/bin/bbjservices --launchd` in the FOREGROUND. Drop the pidfile entirely — the port guard is the only idempotency check.

The new coder_script body contains no literal `${`, so no `$${` Terraform escaping is needed. Shell `$VAR` (e.g. `$HOME`) stays as-is.
  </action>
  <verify>
    <automated>cd /Users/beff/coder && grep -q 'resource "coder_script" "bbjservices"' templates/bbj-services/main.tf && ! grep -q 'bbjservices.pid' templates/bbj-services/main.tf && ! grep -q 'nohup setsid' templates/bbj-services/main.tf && grep -q 'exec sudo /opt/bbx/bin/bbjservices --launchd' templates/bbj-services/main.tf && command -v terraform >/dev/null 2>&1 && terraform -chdir=templates/bbj-services fmt -check || echo "fmt check skipped/failed — run terraform fmt if available"</automated>
  </verify>
  <done>
The `startup_script` no longer contains the background BBjServices launch (no `nohup setsid`, no `/tmp/bbjservices.pid`); it ends with the adapted NOTE comment. A `coder_script.bbjservices` resource exists immediately before `docker_volume.home_volume`, wired to `coder_agent.main.id`, running BBjServices via `exec sudo` with the `:8888` port guard as the only idempotency check. `terraform fmt` reports no changes (2-space indent, aligned `=`).
  </done>
</task>

</tasks>

<verification>
- `grep 'resource "coder_script" "bbjservices"' templates/bbj-services/main.tf` returns a match.
- `grep -c 'bbjservices.pid' templates/bbj-services/main.tf` returns 0.
- `grep -c 'nohup setsid' templates/bbj-services/main.tf` returns 0.
- `terraform -chdir=templates/bbj-services fmt -check` exits 0 (or terraform absent → skip).
- The new resource sits immediately before `resource "docker_volume" "home_volume"`.
</verification>

<success_criteria>
BBjServices is launched by a dedicated `coder_script.bbjservices` (foreground `exec sudo`, `:8888` port guard, no pidfile) instead of a backgrounded job in `startup_script`. The startup_script carries an adapted NOTE explaining the move. File passes `terraform fmt`.
</success_criteria>

<output>
Create `.planning/quick/260716-9pg-convert-bbj-services-bbjservices-launch-/260716-9pg-SUMMARY.md` when done.
</output>
