---
quick_id: 260619-b1h
title: Enable subdomain apps — macOS nip.io recipe + Apache wildcard proxy
date: 2026-06-19
status: complete
---

# Quick Task 260619-b1h

Enable subdomain apps (#1 fix for path-prefix breakage of absolute-URL apps).

## Task
- Document enabling CODER_ACCESS_URL + CODER_WILDCARD_ACCESS_URL (compose already
  forwards both).
- Local macOS recipe: LAN-IP nip.io wildcard (browser + container reachable).
- Production Apache wildcard vhost satisfying the reverse-proxy contract.

## Verify
- Anchors resolve; Apache config meets Host/X-Forwarded-Proto/WebSocket+DERP/
  wildcard-TLS requirements.

## Done
Operators can enable subdomain apps locally (nip.io) and in production (Apache).
