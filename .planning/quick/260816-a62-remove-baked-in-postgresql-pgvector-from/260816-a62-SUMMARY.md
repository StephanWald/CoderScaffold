---
phase: 260816-a62
plan: "01"
subsystem: templates/python-ai
tags: [postgres, pgvector, cleanup, terraform, dockerfile]
status: complete

dependency_graph:
  requires: [260816-7c0]  # docker-outside-of-docker — the reason postgres is no longer needed baked in
  provides: []
  affects: [templates/python-ai/Dockerfile, templates/python-ai/main.tf, templates/python-ai/README.md]

tech_stack:
  removed:
    - postgresql + postgresql-contrib (apt)
    - postgresql-$PG_MAJOR-pgvector (apt)
    - /etc/profile.d/20-postgres.sh (login-shell env shim)
  patterns:
    - Removal-only change; no new dependencies added

key_files:
  modified:
    - templates/python-ai/Dockerfile
    - templates/python-ai/main.tf
    - templates/python-ai/README.md

decisions:
  - "Postgres baked-in feature removed: DooD (docker-outside-of-docker) now provides a container path, making the per-boot baked cluster redundant"
  - "Docker CLI comment rewritten to be self-contained (no back-reference to deleted Postgres block)"
  - "Terraform validate + fmt -check gate passed before final commit"

metrics:
  duration: ~10min
  completed: "2026-08-16"
  tasks: 3
  commits: 3

actuals:
  tokens: 9000
  tasks: 3
  commits: 3

estimate:
  tokens: 45000
  tasks: 3
---

# Phase 260816-a62 Plan 01: Remove Baked-in PostgreSQL + pgvector from python-ai Template Summary

**One-liner:** Cleanly removed the baked PostgreSQL + pgvector feature from all three python-ai template files — Dockerfile apt block, profile.d shim, startup_script cluster manager, env entries, README section — with `terraform fmt -check` and `terraform validate` both passing.

## What Was Done

### Task 1: Dockerfile — PostgreSQL block removed + Docker CLI comment fixed

Deleted the entire `# ── PostgreSQL + pgvector ──` RUN block (~26 lines):
- `apt-get install postgresql postgresql-contrib`
- `PG_MAJOR` resolution and `apt-get install postgresql-$PG_MAJOR-pgvector`
- `pg_dropcluster --stop`
- `install -d ... /var/run/postgresql` (chown)
- `/etc/profile.d/20-postgres.sh` heredoc (PGDATA, DATABASE_URL, PATH exports)
- `initdb --version` check

Fixed the Docker-CLI comment: replaced "see the PostgreSQL block above for why (no NET_ADMIN, no namespace creation inside this workspace container)" with self-contained explanation — no dangling back-reference.

### Task 2: main.tf — startup_script block, env entries removed

(a) Deleted the entire startup_script Postgres block (~43 lines):
- PGDATA_DIR / PG_BIN resolution
- `/var/run/postgresql` writability sudo fix
- First-start `initdb`
- Idempotent `pg_ctl start`
- `createdb coder`
- `CREATE EXTENSION IF NOT EXISTS vector` psql call
- WARN else-branch

The MemPalace-init block's closing `fi` now flows directly into the closing `EOT`; `set -e` flow is intact.

(b) Deleted `PGDATA` and `DATABASE_URL` env entries along with their explanatory comment block.

(c) Scanned header (lines 1–38): no Postgres/pgvector wording found; header left as-is.

Docker DooD feature verified intact: `docker_socket`, `docker_group_id`, `/var/run/docker.sock` mount, `group_add` all untouched.

### Task 3: README — What-it-provides bullet, dedicated section, 3 checklist items removed

- Deleted "PostgreSQL + pgvector" bullet from `## What it provides`
- Deleted entire `## PostgreSQL + pgvector` section (What ships, Connecting, Why it isn't a container, Persistence model, Ops one-liners, Scope)
- Deleted three Postgres-specific checklist items from DEFERRED LIVE-VERIFICATION:
  - `Workspace start brings Postgres up automatically; psql -d coder -c 'SELECT 1' succeeds`
  - `SELECT extversion FROM pg_extension WHERE extname='vector'` returns a version
  - Stop and restart persistence gate (Postgres table survives restart)
- One blank line between `## Why toolchains live in /opt` and `## Cloning a private repository (SSH)`
- Docker/DooD, SSH, push sections and all non-Postgres checklist items intact

## Verification Results

### Zero-match grep (combined across all three files)

```
grep -rniE 'postgres|pgvector|pgdata|pg_ctl|initdb|createdb|DATABASE_URL|20-postgres|CREATE EXTENSION|pg_extension|/var/run/postgresql' \
  templates/python-ai/Dockerfile templates/python-ai/main.tf templates/python-ai/README.md
```

**Result: no output (exit 1 = no matches).** All Postgres tokens eliminated.

### Dockerfile additional checks

- Zero matches on all Postgres tokens: PASS
- No "see the PostgreSQL block above" back-reference: PASS
- `USER coder` retained at end of file: PASS

### main.tf additional checks

- Zero matches on all Postgres tokens: PASS
- `group_add` present: PASS
- `docker.sock` present: PASS

### Terraform gate

```
terraform fmt          # OK (no changes needed)
terraform fmt -check   # exit 0
terraform init -backend=false  # Terraform successfully initialized
terraform validate     # Success! The configuration is valid.
```

All four Terraform commands PASSED.

## Commits

| # | Hash | Description |
|---|------|-------------|
| 1 | c57ee9b | feat(260816-a62-01): remove PostgreSQL + pgvector from python-ai Dockerfile |
| 2 | 1f8ad52 | feat(260816-a62-01): remove PostgreSQL + pgvector from python-ai main.tf |
| 3 | 97be2e6 | feat(260816-a62-01): remove PostgreSQL + pgvector from python-ai README |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs / Deferred Items

None created by this change.

## Deferred Live Verification (REQUIRED before trusting in production)

Per project memory "Infra needs a live deploy gate": static checks (`terraform fmt -check`, `terraform validate`) are necessary but not sufficient. The following must be completed on a real Coder host with a running server and `coder` CLI:

1. `coder templates push python-ai --directory templates/python-ai/ -y` — confirm the push succeeds and the image BUILDS successfully with the Postgres block removed
2. Create a workspace (any python_version) — confirm the workspace STARTS cleanly:
   - No `startup_script` errors referencing Postgres (no PGDATA_DIR, PG_BIN, pg_ctl, createdb, or pgvector log lines)
   - Agent env has no `PGDATA` or `DATABASE_URL` entries (verify with `env | grep -E 'PGDATA|DATABASE_URL'` — expected: no output)
   - All non-Postgres tools still present: `python --version`, `uv --version`, `ruff --version`, `jupyter --version`, `docker --version`, `docker compose version`
3. Second start (workspace stop + start) — confirm idempotence gate: no startup_script errors, all tools still on PATH

## Self-Check: PASSED

- [x] templates/python-ai/Dockerfile — exists, zero Postgres tokens, no back-reference, USER coder present
- [x] templates/python-ai/main.tf — exists, zero Postgres tokens, group_add + docker.sock intact
- [x] templates/python-ai/README.md — exists, zero Postgres tokens, Docker/SSH/push sections intact
- [x] Commits c57ee9b, 1f8ad52, 97be2e6 — all present in git log
- [x] terraform fmt -check: exit 0
- [x] terraform validate: Success!
- [x] Combined zero-match grep: no output (exit 1)
