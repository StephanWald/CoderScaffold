---
phase: 260816-a62
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/python-ai/Dockerfile
  - templates/python-ai/main.tf
  - templates/python-ai/README.md
autonomous: true
requirements: [QUICK-260816-a62]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Dockerfile no longer installs postgresql/postgresql-contrib/pgvector and has no /etc/profile.d/20-postgres.sh"
    - "main.tf startup_script has no Postgres start block and set -e flow is intact through the closing EOT"
    - "coder_agent.main env map has no PGDATA or DATABASE_URL entries; GIT_*/CLAUDE_CONFIG_DIR/UV_* remain"
    - "README no longer advertises PostgreSQL + pgvector anywhere"
    - "terraform fmt -check and terraform validate both pass on templates/python-ai"
    - "grep of the three files finds zero Postgres-related tokens"
  artifacts:
    - templates/python-ai/Dockerfile
    - templates/python-ai/main.tf
    - templates/python-ai/README.md
  key_links:
    - "Dockerfile Docker-CLI comment block is self-contained (no dangling 'see the PostgreSQL block above')"
    - "main.tf heredoc: MemPalace-init block flows directly into closing EOT with no orphaned shell fragments"
    - "Docker feature (docker_socket, docker_group_id, /var/run/docker.sock mount, group_add) untouched"
---

<objective>
Cleanly remove the baked-in PostgreSQL + pgvector feature from the python-ai workspace template. Postgres was baked in only because nested Docker was structurally unusable; now that docker-outside-of-docker is configured (host socket mounted, Docker CLI + Compose in the base image), pgvector can run as a container, so the baked cluster is redundant.

Purpose: eliminate a redundant, per-boot-cost baked database now that a container path exists.
Output: three edited template files (Dockerfile, main.tf, README.md) with all Postgres/pgvector artifacts removed and Terraform still valid.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@templates/python-ai/Dockerfile
@templates/python-ai/main.tf
@templates/python-ai/README.md

Removal-only change. All exact line targets are named in each task action. Do NOT re-introduce any removed content. Do NOT add a Docker apt-install block (the base image already ships the Docker CLI + Compose plugin).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Remove Postgres from Dockerfile + fix Docker-CLI cross-reference</name>
  <files>templates/python-ai/Dockerfile</files>
  <action>
Delete the entire PostgreSQL + pgvector RUN block including its comment header — everything from the line `# ── PostgreSQL + pgvector ─...` (the comment header starting near line 150) through the final `"/usr/lib/postgresql/$PG_MAJOR/bin/initdb" --version` line and its trailing blank line (through ~line 176). This removes: the multi-line comment header, `apt-get install postgresql postgresql-contrib`, the `PG_MAJOR` resolution, `apt-get install postgresql-$PG_MAJOR-pgvector`, `pg_dropcluster`, `install -d ... /var/run/postgresql`, the `/etc/profile.d/20-postgres.sh` heredoc (PGDATA/DATABASE_URL/PATH exports), and the `initdb --version` check.

After deletion, the GitHub CLI (gh) RUN block must flow directly into the `# ── Docker CLI + Compose plugin (docker-outside-of-docker) ──` comment block with exactly one blank line between them.

CROSS-REFERENCE FIX: in the Docker CLI comment block, the sentence currently reads: "A nested engine is NOT used and would NOT work — see the PostgreSQL block above for why (no NET_ADMIN, no namespace creation inside this workspace container)." Since the Postgres block is deleted, rewrite this to be self-contained with the reason stated inline and no back-reference. Replace with a sentence conveying: a nested engine is NOT used and would NOT work here — this workspace container has no NET_ADMIN and cannot create namespaces, so a nested Docker daemon cannot run. Keep the rest of that comment block (the "Because the CLI talks to the host daemon" list, operator gotchas (a)/(b)) and the final `USER coder` line unchanged.

Do NOT touch any other RUN block (Node, uv, CPython, dev tooling, AI SDKs, JupyterLab, SSH host keys, MemPalace, gh).
  </action>
  <verify>
    <automated>grep -niE 'postgres|pgvector|pgdata|pg_ctl|initdb|createdb|DATABASE_URL|20-postgres|CREATE EXTENSION|/var/run/postgresql' templates/python-ai/Dockerfile; test $(grep -icE 'postgres|pgvector|pgdata|pg_ctl|initdb|createdb|DATABASE_URL|20-postgres|CREATE EXTENSION|/var/run/postgresql' templates/python-ai/Dockerfile) -eq 0 && grep -q 'see the PostgreSQL block above' templates/python-ai/Dockerfile && echo FAIL-BACKREF || echo OK</automated>
  </verify>
  <done>Dockerfile has zero Postgres-token matches, retains `USER coder` at the end, and the Docker-CLI comment has no "see the PostgreSQL block above" back-reference.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Postgres from main.tf (startup_script block, env entries, header)</name>
  <files>templates/python-ai/main.tf</files>
  <action>
(a) In the coder_agent.main `startup_script` heredoc, delete the entire PostgreSQL block: from the comment line `# ── PostgreSQL + pgvector — start on the persistent home volume ───...` (near line 363) through the closing `fi` of the final `else echo "WARN: PostgreSQL not found..."` branch (near line 405, the `fi` after `echo "WARN: PostgreSQL not found in the image; skipping database setup" >&2`). Removed content: PGDATA_DIR/PG_BIN resolution, the `/var/run/postgresql` writability sudo fix, first-start `initdb`, idempotent `pg_ctl start`, `createdb coder`, the `CREATE EXTENSION IF NOT EXISTS vector` psql call, and the trailing WARN else-branch.

After deletion, the preceding MemPalace-init block (ending with its own `fi` near line 361) must flow directly into the closing `  EOT` (line 406) of the heredoc. Verify no orphaned shell fragments remain and `set -e` flow is intact — the last executable line before EOT should be the MemPalace-init block's closing `fi`.

(b) In the `env = { ... }` map, delete the PGDATA and DATABASE_URL entries AND their explanatory comment (the comment starting `# Postgres is set system-wide in /etc/profile.d/20-postgres.sh ...` through the `DATABASE_URL = "postgresql://coder@localhost:5432/coder"` line, ~lines 425-430). Keep the trailing blank line handling clean so the closing `}` of the env map is well-formed. Leave GIT_*, CLAUDE_CONFIG_DIR, and UV_* entries untouched.

(c) In the top-of-file header comment: remove any PostgreSQL/pgvector mention from the description / `# Requires:` / `# Resources:` area so the header no longer advertises Postgres. Inspect lines 1-38 for Postgres wording (currently the pre-loaded list and resource list do not name Postgres, but scan and remove any Postgres/pgvector/vector-store phrasing if present). If none exists, leave the header as-is — do not invent removals.

DO NOT touch: the `docker_socket` variable, the `docker_group_id` variable, the `/var/run/docker.sock` volume mount, `group_add`, or any other resource/module. Those are the Docker feature and must remain.
  </action>
  <verify>
    <automated>grep -niE 'postgres|pgvector|pgdata|pg_ctl|initdb|createdb|DATABASE_URL|20-postgres|CREATE EXTENSION|/var/run/postgresql' templates/python-ai/main.tf; test $(grep -icE 'postgres|pgvector|pgdata|pg_ctl|initdb|createdb|DATABASE_URL|20-postgres|CREATE EXTENSION|/var/run/postgresql' templates/python-ai/main.tf) -eq 0 && grep -q 'group_add' templates/python-ai/main.tf && grep -q 'docker.sock' templates/python-ai/main.tf && echo OK || echo FAIL</automated>
  </verify>
  <done>main.tf has zero Postgres-token matches; the heredoc closes cleanly at EOT with intact set -e flow; the env map has no PGDATA/DATABASE_URL; docker_socket/docker_group_id/docker.sock mount/group_add all still present.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Postgres from README + run Terraform verification gate</name>
  <files>templates/python-ai/README.md</files>
  <action>
(a) In `## What it provides` (~line 35), delete the `- **PostgreSQL + pgvector** — a persistent local vector store...` bullet in full.

(b) Delete the entire `## PostgreSQL + pgvector` section (~lines 45-84): the heading, all prose subsections (What ships / Connecting / Why it isn't a container / Persistence model), the "Ops one-liners" fenced code block, and the "Scope" paragraph — through to (but not including) the `## Cloning a private repository (SSH)` heading. Ensure exactly one blank line separates the preceding `## Why toolchains live in /opt` section from `## Cloning a private repository (SSH)`.

(c) In `## DEFERRED LIVE-VERIFICATION CHECKLIST`, delete the three Postgres-specific checkbox items:
- the `Workspace start brings Postgres up automatically; psql -d coder -c 'SELECT 1' succeeds` item (~line 138)
- the `SELECT extversion FROM pg_extension WHERE extname='vector'` item (~line 139)
- the persistence-gate item `Stop and restart the workspace: a table created before the restart is still present (persistence gate)` (~line 140) — this item is Postgres-specific (it verifies a Postgres table survives restart), so remove it.
Keep all other checklist items (push, python_version builds, tool PATH, IDE buttons, JupyterLab, module pins, Claude/MCP/GSD, and the idempotence-gate item on ~line 141).

Keep ALL Docker/DooD, private-repo SSH, and push/configure content intact.
  </action>
  <verify>
    <automated>grep -niE 'postgres|pgvector|pgdata|pg_ctl|initdb|createdb|DATABASE_URL|20-postgres|CREATE EXTENSION|pg_extension|/var/run/postgresql' templates/python-ai/README.md; test $(grep -icE 'postgres|pgvector|pgdata|pg_ctl|initdb|createdb|DATABASE_URL|20-postgres|CREATE EXTENSION|pg_extension|/var/run/postgresql' templates/python-ai/README.md) -eq 0 && echo OK || echo FAIL</automated>
  </verify>
  <done>README has zero Postgres-token matches; Docker/DooD, SSH, and push sections intact; checklist retains all non-Postgres items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace container → host Docker daemon | DooD socket mount (unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260816-a62-01 | Tampering | main.tf heredoc integrity | medium | mitigate | Verify closing EOT and set -e flow intact after block removal (Task 2 verify); terraform validate gate confirms HCL well-formed |
| T-260816-a62-02 | Denial of Service | accidental removal of Docker DooD feature | high | mitigate | Task 2 verify asserts group_add + docker.sock still present; explicit DO-NOT-TOUCH list on Docker resources |

No package installs in this plan (removal-only), so no package-legitimacy gate applies.
</threat_model>

<verification>
After all three tasks, run the Terraform gate from the template directory (project memory "infra-needs-live-deploy-gate": static checks are necessary but not sufficient):

```bash
cd templates/python-ai
terraform fmt                 # leave formatted
terraform fmt -check          # must exit 0
terraform init -backend=false # provider/module init, no remote backend
terraform validate            # must print "Success!"
```

Then the combined zero-match grep across all three files:

```bash
grep -rniE 'postgres|pgvector|pgdata|pg_ctl|initdb|createdb|DATABASE_URL|20-postgres|CREATE EXTENSION|pg_extension|/var/run/postgresql' \
  templates/python-ai/Dockerfile templates/python-ai/main.tf templates/python-ai/README.md
```

Expected: no output (zero matches). Flag any survivor.
</verification>

<success_criteria>
- Dockerfile: PostgreSQL RUN block + comment header removed; Docker-CLI comment self-contained (no back-reference); `USER coder` retained.
- main.tf: startup_script Postgres block removed with intact set -e flow and valid closing EOT; env map free of PGDATA/DATABASE_URL; Docker feature untouched.
- README: What-it-provides bullet, dedicated section, and 3 Postgres checklist items removed; Docker/SSH/push content intact.
- `terraform fmt -check` exits 0; `terraform validate` prints Success.
- Combined grep across the three files returns zero matches.
- LIVE-VERIFY (surface in SUMMARY, likely cannot complete here): `coder templates push python-ai` + create a workspace to confirm the image still BUILDS and the workspace STARTS cleanly with the Postgres block gone (no dangling startup_script errors; agent env has no PGDATA/DATABASE_URL).
</success_criteria>

<output>
Create `.planning/quick/260816-a62-remove-baked-in-postgresql-pgvector-from/260816-a62-SUMMARY.md` when done. In the SUMMARY, explicitly surface the unrun LIVE-VERIFY step (template push + workspace start) as a deferred verification item per project memory "infra-needs-live-deploy-gate".
</output>
