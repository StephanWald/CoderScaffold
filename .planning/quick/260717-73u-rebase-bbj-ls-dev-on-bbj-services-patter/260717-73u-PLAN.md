---
phase: quick-260717-73u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile
  - /Users/beff/coder-bbj-private/templates/bbj-ls-dev/combinations.json
  - /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf
  - /Users/beff/coder-bbj-private/scripts/push-templates.sh
autonomous: true
requirements: [REBASE-LSDEV]
must_haves:
  truths:
    - "The bbj-ls-dev image bakes a complete BBj install at /opt/bbx (coder-owned), like bbj-dev"
    - "bbj-ls-dev main.tf consumes ./modules/bbj-base instead of an inline house pattern"
    - "PRODUCTION obfuscated combo is combinations.json[0] and thus the stack parameter default"
    - "Both JDK 17 and JDK 21 are always installed for the LS toolchain; JDK build-arg selects the /opt/java/default runtime"
    - "Existing workspaces do not lose their home volume (moved blocks preserve state)"
    - "BBjServices is reachable on :8888 via coder_app; npm install still primes bbj-vscode"
    - "push-templates.sh stages Dockerfile.bbj-ls-dev and passes the license-server variable"
  artifacts:
    - path: /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile
      provides: "Multi-stage base -> bbjinstall -> final image with /opt/bbx baked"
      contains: "FROM base AS bbjinstall"
    - path: /Users/beff/coder-bbj-private/templates/bbj-ls-dev/combinations.json
      provides: "Two-combo stack manifest, production-obf first"
      contains: "bbj-26.01-jdk25"
    - path: /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf
      provides: "bbj-base module consumer with BBj image build, stack param, BBjServices"
      contains: "module \"base\""
    - path: /Users/beff/coder-bbj-private/scripts/push-templates.sh
      provides: "Staging + per-template variable arm for bbj-ls-dev"
      contains: "bbj-ls-dev)"
  key_links:
    - from: main.tf module "base"
      to: docker_image.main.image_id
      via: "image_id input"
      pattern: "image_id\\s*=\\s*docker_image.main.image_id"
    - from: main.tf docker_image.main
      to: Dockerfile.bbj-ls-dev in bbj_context_path
      via: "build context + dockerfile"
      pattern: "Dockerfile.bbj-ls-dev"
---

<objective>
Rebase the PRIVATE `bbj-ls-dev` template onto the `bbj-dev` / `bbj-services` pattern: bake a complete BBj install (`/opt/bbx`) into the workspace image via a multi-stage Dockerfile driven from the shared `/mnt/bbj-assets` context, and refactor the template's inline "house pattern" onto the shared `./modules/bbj-base` module.

Purpose: unify bbj-ls-dev with its sibling BBj engineering templates so it gets a live, coder-owned BBjServices for language-server testing, a curated installer/JDK stack parameter, and the shared agent/editor/Claude plumbing from one module — while preserving existing workspaces' persistent home volumes.

Output: reworked `Dockerfile`, new `combinations.json`, reworked `main.tf`, extended `push-templates.sh` — all in `/Users/beff/coder-bbj-private`, committed there.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/beff/coder/CLAUDE.md

Pattern sources (READ these; the tasks below say "copy verbatim from" or "follow" them):
@/Users/beff/coder-bbj-private/templates/bbj-dev/Dockerfile
@/Users/beff/coder-bbj-private/templates/bbj-dev/main.tf
@/Users/beff/coder-bbj-private/templates/bbj-dev/combinations.json
@/Users/beff/coder-bbj-private/modules/bbj-base/main.tf
@/Users/beff/coder-bbj-private/modules/bbj-base/variables.tf
@/Users/beff/coder-bbj-private/modules/bbj-base/outputs.tf
@/Users/beff/coder-bbj-private/scripts/push-templates.sh

Current files being reworked:
@/Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile
@/Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf

CRITICAL — two repos:
- This PLAN.md lives in /Users/beff/coder (public repo). Do NOT commit anything there.
- ALL code changes and the commit go in /Users/beff/coder-bbj-private (private; never push publicly).
- terraform/tofu are NOT on PATH — validation is manual review + `bash -n`. Do NOT attempt `docker build`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-stage Dockerfile + combinations.json</name>
  <files>/Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile, /Users/beff/coder-bbj-private/templates/bbj-ls-dev/combinations.json</files>
  <action>
Rewrite /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile as a three-stage build `base` -> `bbjinstall` -> `final`, following bbj-dev/Dockerfile's structure exactly but keeping bbj-ls-dev's toolchain specifics.

`base` stage (`FROM ${BASE_IMAGE} AS base`, `ARG JDK=adoptium-21`, `ARG MAVEN_VERSION=3.9.16`, `USER root`):
- Keep everything the CURRENT bbj-ls-dev image has: Node.js LTS via NodeSource; pinned Maven ${MAVEN_VERSION}; the profile.d exports (JAVA_HOME=/opt/java/default, MAVEN_HOME=/opt/maven, PATH); the gh CLI apt install block.
- JDK layout: ALWAYS install Temurin 17 AND 21 to /opt/java/17 and /opt/java/21 (the current bbj-ls-dev dual-JDK RUN, lines 45-65 of the current Dockerfile — the LS toolchain needs both). ADDITIONALLY honor the `JDK` build-arg: if `adoptium-21`, `ln -sfn /opt/java/21 /opt/java/default` (no extra download); if `adoptium-25`, also install Temurin 25 to /opt/java/25 (same Adoptium `latest/25/ga` endpoint, arch-detected) and `ln -sfn /opt/java/25 /opt/java/default`; any other value must `echo ERROR ... >&2; exit 1` to fail the build. After selecting default, `update-alternatives --install ... java/javac -> /opt/java/default/bin/...` and `java -version`. Keep the amd64/arm64 `dpkg --print-architecture` case (image is effectively amd64 via bbj_platform, but keep the detection).
- ssh-keyscan block: keep the current bbj-ls-dev version — github.com/gitlab.com/bitbucket.org PLUS `ssh-keyscan -p 10022 ... git.basis.cloud` on port 10022, StrictHostKeyChecking accept-new (current lines 91-102).
- ADD the MemPalace CLI venv block copied verbatim from bbj-dev/Dockerfile lines 133-140 (python3 + python3-venv, /opt/mempalace venv, pip install mempalace, symlink to /usr/local/bin/mempalace). Required because bbj-base's startup script registers the mempalace MCP server.

`bbjinstall` stage: copy VERBATIM from bbj-dev/Dockerfile lines 285-309 (`FROM base AS bbjinstall`; COPY playback.properties; `ARG BBJ_JAR_NAME=BBj.jar`; COPY ${BBJ_JAR_NAME}; COPY certificate.bls; `ARG LICENSE_SERVER` with the empty-warning + sed of LICENSE_SERVER_PLACEHOLDER; the vfork `java -jar /tmp/BBj.jar -p /tmp/playback.properties`; the post-install sed of BBj.properties + the logs/cache/documentation-only trim that KEEPS the full lib set).

`final` stage: `FROM base AS final`; `COPY --from=bbjinstall --chown=coder:coder /opt/bbx /opt/bbx`; `EXPOSE 8888`; `USER coder` as the LAST line. (gh CLI is already in `base` above, so do not duplicate it here.)

Header comment block: replace the current forked-from-java-fullstack note with a note explaining this file is a multi-stage BBj-baking image, is staged into the asset folder as `Dockerfile.bbj-ls-dev` by scripts/push-templates.sh, and the build context is /mnt/bbj-assets. List the build args (BASE_IMAGE, JDK, BBJ_JAR_NAME, MAVEN_VERSION, LICENSE_SERVER). Keep the toolchain-rationale comments (dual JDK for LS, Maven pinned, Gradle from wrapper).

Then CREATE /Users/beff/coder-bbj-private/templates/bbj-ls-dev/combinations.json with the PRODUCTION obfuscated combo FIRST (so locals default to combinations[0].id):
[
  { "id": "bbj-26.01-jdk25", "display": "BBj 26.01 · JDK 25 (Adoptium)", "jar": "BBjBaristaDemos2601_06-30-2026_1027.jar", "jdk": "adoptium-25" },
  { "id": "bbj-trunk-nonobf-jdk21", "display": "BBj trunk non-obf (000) · JDK 21", "jar": "BBjBaristaDemos000_07-13-2026_1256.jar", "jdk": "adoptium-21" }
]
Do NOT introduce any "v1"/"simplified"/"placeholder" reductions — bake the full runtime.
  </action>
  <verify>
    <automated>grep -q 'FROM ${BASE_IMAGE} AS base' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile && grep -q 'FROM base AS bbjinstall' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile && grep -q 'FROM base AS final' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile && grep -q 'chown=coder:coder /opt/bbx' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile && grep -q 'mempalace' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile && grep -q 'adoptium-25' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile && grep -q 'ssh-keyscan -p 10022' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/Dockerfile && python3 -m json.tool /Users/beff/coder-bbj-private/templates/bbj-ls-dev/combinations.json >/dev/null && head -c 200 /Users/beff/coder-bbj-private/templates/bbj-ls-dev/combinations.json | grep -q 'bbj-26.01-jdk25'</automated>
  </verify>
  <done>Dockerfile is three stages, bakes coder-owned /opt/bbx, always installs JDK 17+21, adds MemPalace, keeps port-10022 keyscan; combinations.json is valid JSON with the production-obf combo first.</done>
</task>

<task type="auto">
  <name>Task 2: Rework main.tf onto the bbj-base module</name>
  <files>/Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf</files>
  <action>
Rework /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf following bbj-dev/main.tf's structure. Preserve the terraform{} required_providers block and provider "docker" as-is.

KEEP existing variables: docker_socket, workspace_image, anthropic_api_key, maven_version, ls_repo_https, ls_repo_ssh, bbjls_repo_ssh, bbjls_branch.

ADD BBj variables, copying descriptions verbatim from bbj-dev/main.tf lines 89-123: bbj_context_path (default "/mnt/bbj-assets"), bbj_license_server (default ""), bbj_platform (default "linux/amd64"), bbj_app_subdomain (bool, default true), bbj_host_port (number, default 0), bbj_host_port_bind (default "127.0.0.1").

ADD `data "coder_parameter" "bbj_stack"` mirroring bbj-dev lines 141-158 (display_name "BBj runtime", default local.bbj_combinations[0].id, mutable false, dynamic option over local.bbj_combinations). Adjust the description text to bbj-ls-dev context (which BBj installer+JDK combo to bake as /opt/bbx for LS testing; keep combinations.json in sync).

Data sources: keep data "coder_workspace" "me" and data "coder_workspace_owner" "me" (needed for the image name). The bbj-base module has its own copies — that is intentional and fine. Remove data "coder_provisioner" "me" from the root (the module owns the agent now).

locals: replace the current locals with — repos_dir = "/home/coder/repos"; and the combinations block copied from bbj-dev lines 186-194 shape: default_combinations fallback list matching combinations.json[1] (the non-obf trunk entry) as the safety fallback, bbj_combinations = try(jsondecode(file("${path.module}/combinations.json")), local.default_combinations), combos_by_id, selected = combos_by_id[data.coder_parameter.bbj_stack.value]. Drop claude_volume_name/username (the module owns those).

docker_image.main: name = "coder-${data.coder_workspace.me.id}-bbjlsdev-${data.coder_parameter.bbj_stack.value}"; build context = var.bbj_context_path, dockerfile = "Dockerfile.bbj-ls-dev", platform = var.bbj_platform; build_args = BASE_IMAGE=var.workspace_image, JDK=local.selected.jdk, BBJ_JAR_NAME=local.selected.jar, MAVEN_VERSION=var.maven_version, LICENSE_SERVER=var.bbj_license_server. triggers mirror bbj-dev lines 219-228: dockerfile_sha1 = try(filesha1("${var.bbj_context_path}/Dockerfile.bbj-ls-dev"), filesha1("${path.module}/Dockerfile")), stack, jdk, maven_version, license_server, platform, bbj_jar_sha1 = try(filesha1("${var.bbj_context_path}/${local.selected.jar}"), "no-jar"). keep_locally = true.

DELETE the inline house pattern from the current main.tf: resource "coder_agent" "main" (the entire startup_script + env + metadata), resource "docker_volume" "home_volume", resource "docker_container" "workspace", module "code-server", module "jetbrains-gateway", module "claude-code", and the claude/username locals.

ADD `module "base"` (source "./modules/bbj-base", NO count) mirroring bbj-dev lines 239-266:
- image_id = docker_image.main.image_id
- anthropic_api_key = var.anthropic_api_key
- code_server_folder = "/home/coder/repos"  (opens the parent holding both checkouts)
- jetbrains_folder = "/home/coder/repos/bbj-language-server"  (IntelliJ on the LS project — this is why the module keeps the folders separate)
- extra_env = { JAVA_HOME = "/opt/java/default", MAVEN_HOME = "/opt/maven", BBJ_HOME = "/opt/bbx" }
- port_internal = 8888, port_external = var.bbj_host_port, port_bind = var.bbj_host_port_bind
- (no extra_volumes — bbj-ls-dev has no per-owner svn volume)
- extra_startup_script = a heredoc containing ONLY the bbj-ls-dev-specific pieces moved out of the current inline startup_script: (a) the Gradle toolchain gradle.properties write from current lines 216-223 (mkdir ~/.gradle; append org.gradle.java.installations.paths=/opt/java/17,/opt/java/21 if absent); (b) the two idempotent non-fatal repo clones from current lines 225-251 — bbj-language-server HTTPS clone + `remote set-url --push origin` flip to SSH, and the bbj-ls SSH clone of ${var.bbjls_branch} from the BASIS forge with the full WARN guidance lines; and (c) keep the trailing NOTE comment (current lines 253-256) explaining npm install runs as its own coder_script. Do NOT duplicate anything the module already provides (skel seed, Claude config/symlinks, CLAUDE_CONFIG_DIR, bypassPermissions, webforJ/MemPalace MCP registration, GSD install). NOTE on interpolation: extra_startup_script is a Terraform string interpolated inside the module's heredoc, so keep the ${var.ls_repo_https} / ${local.repos_dir} / ${var.bbjls_branch} references as Terraform interpolations (same as they are now) and preserve any `$${...}` shell escaping.

KEEP resource "coder_script" "npm_install_bbj_vscode" as-is but change agent_id = module.base.agent_id (was coder_agent.main.id). local.repos_dir is still referenced there — ensure it remains defined.

ADD resource "coder_script" "bbjservices" copied verbatim from bbj-dev/main.tf lines 324-345, with agent_id = module.base.agent_id.

ADD resource "coder_app" "bbjservices" copied verbatim from bbj-dev/main.tf lines 349-363, with agent_id = module.base.agent_id and subdomain = var.bbj_app_subdomain.

ADD terraform `moved` blocks for state safety (the home-volume move is critical — prevents destroy/create data loss):
  moved { from = coder_agent.main,            to = module.base.coder_agent.main }
  moved { from = docker_volume.home_volume,   to = module.base.docker_volume.home_volume }
  moved { from = docker_container.workspace,  to = module.base.docker_container.workspace }
  moved { from = module.code-server,          to = module.base.module.code-server }
  moved { from = module.jetbrains-gateway,    to = module.base.module.jetbrains-gateway }
  moved { from = module.claude-code,          to = module.base.module.claude-code }

Update the header comment block: keep the dev-loop docs; add that BBjServices now runs on :8888 (coder_app), the stack parameter selects the baked BBj install, the image builds from the /mnt/bbj-assets asset context (Dockerfile.bbj-ls-dev), and the image is amd64 (bbj_platform).
  </action>
  <verify>
    <automated>grep -q 'source *= *"./modules/bbj-base"' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf && grep -q 'Dockerfile.bbj-ls-dev' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf && grep -q 'coder_app" "bbjservices"' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf && grep -q 'module.base.docker_volume.home_volume' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf && grep -q 'module.base.agent_id' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf && ! grep -q 'resource "coder_agent"' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf && ! grep -q 'resource "docker_volume" "home_volume"' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf && grep -q 'org.gradle.java.installations.paths=/opt/java/17,/opt/java/21' /Users/beff/coder-bbj-private/templates/bbj-ls-dev/main.tf</automated>
  </verify>
  <done>main.tf consumes module.base, builds the BBj image from the asset context, wires npm/bbjservices scripts + app to module.base.agent_id, defines all six moved blocks, and no longer declares an inline agent/home volume/container/editor modules.</done>
</task>

<task type="auto">
  <name>Task 3: Extend push-templates.sh, review, and commit</name>
  <files>/Users/beff/coder-bbj-private/scripts/push-templates.sh</files>
  <action>
Edit /Users/beff/coder-bbj-private/scripts/push-templates.sh:

1. Asset-staging section (currently lines 68-89): in addition to staging templates/bbj-dev/Dockerfile -> ${ASSETS}/Dockerfile.bbj-dev, ALSO stage templates/bbj-ls-dev/Dockerfile -> ${ASSETS}/Dockerfile.bbj-ls-dev with an analogous `echo "Staging ..."` + `cp` pair. Update the section comment to mention both templates share the asset context.

2. Per-template case block (currently lines 139-154): add a `bbj-ls-dev)` arm mirroring the `bbj-dev)` arm — pass `--variable bbj_license_server=${BBJ_LICENSE_SERVER}` when BBJ_LICENSE_SERVER is set (same WARN when unset), and optionally `--variable bbj_host_port=${BBJ_LS_DEV_HOST_PORT}` when that env var is set. Keep the `bbj-dev)` arm unchanged.

Then run the validation pass (terraform/tofu are not installed):
- `bash -n /Users/beff/coder-bbj-private/scripts/push-templates.sh` must pass (no syntax errors).
- Manually re-read the reworked main.tf and Dockerfile against the bbj-dev sources for HCL/Docker correctness: balanced braces, every `module.base.*` reference resolves, every `local.*` used is defined, no leftover references to the deleted coder_agent.main / docker_volume.home_volume / claude_volume_name, and the `moved` block targets match the module's resource addresses.

Finally, commit in the PRIVATE repo only:
- `git -C /Users/beff/coder-bbj-private add templates/bbj-ls-dev/Dockerfile templates/bbj-ls-dev/combinations.json templates/bbj-ls-dev/main.tf scripts/push-templates.sh`
- `git -C /Users/beff/coder-bbj-private commit` with message `feat(bbj-ls-dev): rebase on bbj-services pattern; bake /opt/bbx and adopt bbj-base module` plus the Co-Authored-By trailer.
- Do NOT push. Do NOT commit anything in /Users/beff/coder.
  </action>
  <verify>
    <automated>bash -n /Users/beff/coder-bbj-private/scripts/push-templates.sh && grep -q 'Dockerfile.bbj-ls-dev' /Users/beff/coder-bbj-private/scripts/push-templates.sh && grep -q 'bbj-ls-dev)' /Users/beff/coder-bbj-private/scripts/push-templates.sh && grep -q 'BBJ_LS_DEV_HOST_PORT' /Users/beff/coder-bbj-private/scripts/push-templates.sh && git -C /Users/beff/coder-bbj-private log -1 --pretty=%s | grep -q 'bbj-ls-dev' && git -C /Users/beff/coder status --porcelain | grep -qv 'coder-bbj-private'</automated>
  </verify>
  <done>push-templates.sh stages Dockerfile.bbj-ls-dev and has a bbj-ls-dev variable arm; `bash -n` passes; manual HCL review done; a single conventional commit exists in coder-bbj-private and nothing was committed in the public coder repo.</done>
</task>

</tasks>

<verification>
- `python3 -m json.tool combinations.json` valid; production-obf combo is element [0].
- Dockerfile: three named stages; `COPY --from=bbjinstall --chown=coder:coder /opt/bbx /opt/bbx`; JDK 17+21 always present; adoptium-25 branch + unknown-JDK failure; MemPalace venv; port-10022 keyscan; `USER coder` last.
- main.tf: `module "base"` wired to docker_image.main.image_id; image built from Dockerfile.bbj-ls-dev in var.bbj_context_path; six `moved` blocks (home_volume move present); npm/bbjservices scripts + bbjservices app on module.base.agent_id; no inline agent/volume/container/editor modules remain.
- push-templates.sh: stages both Dockerfiles; `bbj-ls-dev)` arm passes license-server + optional host port; `bash -n` clean.
- Single commit in /Users/beff/coder-bbj-private; zero commits/edits in /Users/beff/coder.
</verification>

<success_criteria>
bbj-ls-dev builds a multi-stage image that bakes a coder-owned /opt/bbx while retaining the full LS toolchain (JDK 17+21, Node, Maven, MemPalace), consumes the shared bbj-base module without losing existing workspaces' home volumes, exposes BBjServices on :8888, defaults to the production-obfuscated stack, and pushes cleanly through the extended push-templates.sh — all committed to the private repo only.
</success_criteria>

<output>
Report completion directly to the orchestrator. No SUMMARY file required for this quick task; the orchestrator handles the planning-repo docs commit.
</output>
