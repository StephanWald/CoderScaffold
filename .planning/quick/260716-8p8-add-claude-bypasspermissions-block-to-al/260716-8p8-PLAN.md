---
phase: quick-260716-8p8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/docker/main.tf
  - templates/coderscaffold/main.tf
  - templates/java-fullstack/main.tf
  - templates/bbj-services/main.tf
autonomous: true
requirements: [QUICK-8p8]
must_haves:
  truths:
    - "Every workspace of an owner starts with claude in bypassPermissions mode without re-approving"
    - "The block writes only to the per-owner shared settings.json, never the operator host"
    - "Re-running a workspace start re-asserts the setting without duplicating or wiping other keys"
  artifacts:
    - path: "templates/docker/main.tf"
      provides: "Claude permissions block in coder_agent startup_script"
      contains: "bypassPermissions"
    - path: "templates/coderscaffold/main.tf"
      provides: "Claude permissions block in coder_agent startup_script"
      contains: "bypassPermissions"
    - path: "templates/java-fullstack/main.tf"
      provides: "Claude permissions block in coder_agent startup_script"
      contains: "bypassPermissions"
    - path: "templates/bbj-services/main.tf"
      provides: "Claude permissions block in coder_agent startup_script"
      contains: "bypassPermissions"
  key_links:
    - from: "coder_agent.main.startup_script"
      to: "$CLAUDE_SHARED/dot-claude/settings.json"
      via: "node -e JSON merge"
      pattern: "defaultMode.*bypassPermissions"
---

<objective>
Add an idempotent "Claude permissions" block to the `coder_agent.main.startup_script`
of all four workspace templates so that `claude` runs as if started with
`--dangerously-skip-permissions` in every workspace of a given owner.

Purpose: developers should not have to re-approve permissions in each new workspace.
The setting is written into the per-owner shared Claude config volume, so it is scoped
to that owner's workspaces and never touches the operator host.

Output: identical "Claude permissions" block inserted into four `main.tf` files,
immediately after the `~/.claude.json` symlink line and before the webforJ MCP block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- The insertion anchor and the pattern to replicate. Same in all four files; -->
<!-- line numbers differ per file — locate by content, not by number. -->

Anchor line (identical text in every file):
    ln -sf "$CLAUDE_SHARED/dot-claude.json" "$HOME/.claude.json"

Immediately followed (in every file) by the webforJ MCP block, which starts with:
    # ── webforJ MCP server — preconfigure in user-scope Claude config ─────────

Reference pattern to mirror (from templates/docker/main.tf, the webforJ block):
- Guard:   if command -v node >/dev/null 2>&1; then ... else echo "WARN: node not found; ..." >&2 ; fi
- Merge:   CLAUDE_JSON=... node -e '<<script>>' || echo "WARN: ...; continuing" >&2
- Script body reads the file with try/catch defaulting to {}, mutates one key, then
  fs.writeFileSync(f, JSON.stringify(cfg, null, 2) + "\n");

KEY DIFFERENCE for the new block: the merge target is the Claude Code SETTINGS file,
NOT dot-claude.json. Target path:  $CLAUDE_SHARED/dot-claude/settings.json
(the dot-claude/ directory already exists — it is created by an earlier `mkdir -p`
and symlinked to $HOME/.claude.)

Terraform heredoc escaping: the startup_script is a <<-EOT block with interpolation.
Shell `$VAR` stays literal, but any literal `${` must be written as `$${`. The existing
node -e blocks contain no `${` — neither should the new one. Keep it consistent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the Claude permissions block into all four templates</name>
  <files>templates/docker/main.tf, templates/coderscaffold/main.tf, templates/java-fullstack/main.tf, templates/bbj-services/main.tf</files>
  <action>
In EACH of the four files, locate the anchor line
`ln -sf "$CLAUDE_SHARED/dot-claude.json" "$HOME/.claude.json"` and the blank line
after it, followed by the webforJ MCP comment
(`# ── webforJ MCP server — preconfigure in user-scope Claude config ...`).
Insert the block below AFTER the symlink line's trailing blank line and BEFORE the
webforJ comment, so it sits between them.

Match the surrounding indentation exactly (the startup_script body is indented; in the
reference the `if command -v node` line is indented 4 spaces). Preserve the existing
box-drawing comment style (`# ── ... ─────`).

Insert this block (adjust only indentation to match each file):

    # ── Claude permissions — force bypassPermissions in every workspace ───────
    # Merges permissions.defaultMode = "bypassPermissions" into the shared Claude
    # Code settings file so `claude` behaves as if launched with
    # --dangerously-skip-permissions in every workspace of THIS owner. Scope: this
    # writes ONLY to $CLAUDE_SHARED/dot-claude/settings.json — the per-owner Docker
    # volume mounted inside workspace containers — never the operator host. Merged
    # with node (guaranteed present in the image) so all other existing settings.json
    # keys are preserved. Idempotent: re-asserts the key on every start. Non-fatal
    # under set -e (WR-03 warn-and-continue): a missing node or a write failure must
    # NEVER abort the startup_script — log and continue.
    if command -v node >/dev/null 2>&1; then
      CLAUDE_SETTINGS="$CLAUDE_SHARED/dot-claude/settings.json" node -e '
        const fs = require("fs");
        const f = process.env.CLAUDE_SETTINGS;
        let cfg = {};
        try { cfg = JSON.parse(fs.readFileSync(f, "utf8") || "{}") || {}; } catch (e) {}
        cfg.permissions = cfg.permissions || {};
        cfg.permissions.defaultMode = "bypassPermissions";
        fs.writeFileSync(f, JSON.stringify(cfg, null, 2) + "\n");
      ' || echo "WARN: could not set Claude bypassPermissions; continuing" >&2
    else
      echo "WARN: node not found; skipping Claude bypassPermissions setup" >&2
    fi

Do NOT introduce any literal `${` in the node script (there is none above — keep it so).
Apply the identical block to all four files.
  </action>
  <verify>
    <automated>test $(grep -rl 'permissions.defaultMode = "bypassPermissions"' templates/docker/main.tf templates/coderscaffold/main.tf templates/java-fullstack/main.tf templates/bbj-services/main.tf | wc -l | tr -d ' ') = 4 && for d in docker coderscaffold java-fullstack bbj-services; do terraform -chdir=templates/$d fmt -check >/dev/null 2>&1 || terraform -chdir=templates/$d validate >/dev/null 2>&1 || echo "check $d manually"; done; echo OK</automated>
  </verify>
  <done>
All four main.tf files contain the Claude permissions block between the
`~/.claude.json` symlink line and the webforJ MCP block; the node snippet targets
`$CLAUDE_SHARED/dot-claude/settings.json`, sets `permissions.defaultMode` to
`bypassPermissions`, preserves other keys, and is non-fatal under set -e.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'bypassPermissions'` returns >=1 in each of the four main.tf files.
- The new block appears strictly between the `ln -sf ... .claude.json` line and the
  `# ── webforJ MCP server` comment in each file.
- No literal `${` was introduced (heredoc interpolation stays intact); running
  `terraform fmt -check` / `terraform validate` in each template dir does not error on
  the change.
</verification>

<success_criteria>
All four workspace templates idempotently set `permissions.defaultMode =
"bypassPermissions"` in the per-owner shared `settings.json` on every workspace start,
using the same guarded, non-fatal `node -e` merge pattern as the existing webforJ MCP
block, without disturbing any other settings.json keys or the operator host.
</success_criteria>

<output>
Create `.planning/quick/260716-8p8-add-claude-bypasspermissions-block-to-al/260716-8p8-SUMMARY.md` when done
</output>
