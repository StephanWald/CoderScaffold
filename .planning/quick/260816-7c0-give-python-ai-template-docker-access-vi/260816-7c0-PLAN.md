---
phase: quick-260816-7c0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/python-ai/Dockerfile
  - templates/python-ai/main.tf
autonomous: true
requirements: [DOOD-01]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Inside a python-ai workspace, `docker ps` and `docker compose up` talk to the HOST daemon (docker-outside-of-docker), running compose services as sibling containers."
    - "The coder user can access the mounted host socket because the container joins the host's docker group GID."
    - "`terraform fmt -check` and `terraform validate` pass against templates/python-ai/ with no diff and no errors."
  artifacts:
    - "templates/python-ai/Dockerfile — Docker CLI + Compose plugin install block (docker-ce-cli + docker-compose-plugin only), placed before the final USER coder line."
    - "templates/python-ai/main.tf — docker_group_id variable + socket volume mount + group_add on docker_container.workspace."
  key_links:
    - "docker_group_id default (999) must match the host `stat -c '%g' /var/run/docker.sock` — otherwise the coder user cannot access the socket (operator must override)."
    - "main.tf socket volume mount ↔ Dockerfile docker-ce-cli: the CLI is useless without the mounted socket, the socket is useless without the CLI."
---

<objective>
Give the python-ai workspace template Docker access via the Coder-recommended
socket-mount ("docker-outside-of-docker") variant: install the Docker CLI +
Compose plugin in the image, mount the host `/var/run/docker.sock` into the
workspace container, and join the host docker group so the coder user can use
the socket. `docker` / `docker compose` inside the workspace then drive the
HOST daemon; compose stacks run as sibling containers.

Purpose: developers in python-ai workspaces need `docker` and `docker compose`
for containerized apps and test dependencies, without a nested engine (which is
structurally unusable in this container — see the existing PostgreSQL block note).

Output: edited templates/python-ai/Dockerfile and templates/python-ai/main.tf,
statically verified with `terraform fmt -check` + `terraform validate`; a
LIVE-VERIFY deploy gate surfaced in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@templates/python-ai/Dockerfile
@templates/python-ai/main.tf

# Convention to mirror for group_add + docker-socket-GID discovery:
@templates/docker/main.tf
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add Docker CLI + Compose plugin install block to the python-ai Dockerfile (docker-outside-of-docker)</name>
  <files>templates/python-ai/Dockerfile</files>
  <action>
Insert a new build block into templates/python-ai/Dockerfile, placed AFTER the
PostgreSQL + pgvector block (which ends at the `initdb --version` line) and
BEFORE the final `USER coder` line. Mirror the exact style of the existing `gh`
apt block and the PostgreSQL block: a section header comment fence
(`# ── Docker CLI + Compose plugin (docker-outside-of-docker) ──…`), a keyring
placed under /etc/apt/keyrings, an arch-aware sources line using
`$(dpkg --print-architecture)`, and a closing `apt-get clean && rm -rf /var/lib/apt/lists/*`.

The block must:
  - `mkdir -p -m 755 /etc/apt/keyrings` (or reuse the dir — it already exists
    from the gh block; recreating with -p is idempotent and matches the gh
    block's own `mkdir -p -m 755`), fetch Docker's official GPG key from
    https://download.docker.com/linux/ubuntu/gpg into
    /etc/apt/keyrings/docker.gpg, and `chmod go+r /etc/apt/keyrings/docker.gpg`.
  - Write the apt source to /etc/apt/sources.list.d/docker.list with
    `arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg`
    pointing at `https://download.docker.com/linux/ubuntu`. The base image is
    codercom/enterprise-base:ubuntu; use the Ubuntu codename via
    `$(. /etc/os-release && echo "$VERSION_CODENAME")` for the suite, `stable`
    for the component — matching Docker's documented apt setup.
  - `apt-get update` then `apt-get install -y --no-install-recommends docker-ce-cli docker-compose-plugin`.
    Install ONLY those two packages — do NOT install docker-ce, containerd.io,
    or docker-buildx-plugin: this is docker-outside-of-docker, the daemon is the
    HOST's, so no engine or container runtime is installed in the image.
  - End with `apt-get clean` and `rm -rf /var/lib/apt/lists/*` (mirror the gh block).

Comment content (directive prose in the header fence, NOT a fenced code block):
explain that this is docker-outside-of-docker — the CLI + Compose plugin talk to
the HOST Docker daemon via the socket mounted by main.tf (not a nested engine);
compose stacks run as SIBLING containers alongside the workspace, not children;
and note the two operator gotchas: (a) bind-mount paths in compose files resolve
against the HOST filesystem, not the workspace filesystem; (b) socket access
grants effective root on the host, acceptable for a single operator but not for
a shared/multi-tenant team.

Do NOT weaken or contradict the existing PostgreSQL block note that
"Docker-in-Docker is structurally unusable inside this workspace container." That
note concerns a NESTED engine and remains true; socket-mount is a different
mechanism (host daemon, not nested). Leave the PostgreSQL block and its
Postgres-baked rationale fully intact — only ADD the new Docker CLI block.
  </action>
  <verify>
    <automated>test -f templates/python-ai/Dockerfile && grep -q 'docker-ce-cli' templates/python-ai/Dockerfile && grep -q 'docker-compose-plugin' templates/python-ai/Dockerfile && grep -Eq 'download\.docker\.com/linux/ubuntu' templates/python-ai/Dockerfile && grep -q '/etc/apt/keyrings/docker.gpg' templates/python-ai/Dockerfile</automated>
  </verify>
  <done>
The Dockerfile installs docker-ce-cli + docker-compose-plugin (and NOT
docker-ce/containerd) from Docker's official apt repo with a keyring under
/etc/apt/keyrings, arch-aware, placed before `USER coder`, ending with
apt-get clean + rm of apt lists. The PostgreSQL DinD-unusable note is untouched.
Verify no engine is installed: `grep -Eq '(^|[^-])docker-ce([^-]|$)|containerd' templates/python-ai/Dockerfile` returns NON-zero (matches only the -cli variant, no bare engine).
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount the host Docker socket + join the docker group in main.tf</name>
  <files>templates/python-ai/main.tf</files>
  <action>
Three edits to templates/python-ai/main.tf, mirroring the documented convention
in templates/docker/main.tf (the group_add + `stat -c '%g' /var/run/docker.sock`
discovery block around lines 419-432):

1. Add a `docker_group_id` variable near the existing `docker_socket` variable
   (after it, in the `# ── Variables ──` section). Type string, default "999".
   Description must document: this is the host's docker group GID; the coder
   user (the image ends with `USER coder`) needs to be in this group to access
   the mounted socket; discover the correct value with
   `stat -c '%g' /var/run/docker.sock` on the host; override it if the host's
   docker socket GID differs from 999 (the common Debian/Ubuntu default).

2. In `resource "docker_container" "workspace"`, add a `volumes` block mounting
   the host socket: `host_path = "/var/run/docker.sock"`,
   `container_path = "/var/run/docker.sock"`, `read_only = false`. Place it AFTER
   the two existing volume blocks (/home/coder and /home/coder/.claude-shared),
   with a short comment: host Docker socket for docker-outside-of-docker — the
   in-image Docker CLI + Compose plugin drive the HOST daemon; compose stacks run
   as sibling containers. Note that `host_path` is a HOST path (bind mount), so
   the socket must exist on the host (it does — the Coder server itself mounts it,
   per the header/README).

3. In the same resource, add `group_add = [var.docker_group_id]` so the coder
   user joins the host docker group and can read/write the socket. Add a brief
   comment referencing the `stat -c '%g' /var/run/docker.sock` discovery and that
   a mismatch causes socket permission-denied (operator overrides docker_group_id).

4. Update the header comment block at the top of main.tf: the `# Requires:`
   section and the `# Resources:`/description area. Add that the host Docker
   socket is now mounted into the workspace and that `docker` / `docker compose`
   are available inside the workspace (docker-outside-of-docker — host daemon,
   sibling containers). Keep all existing header lines intact.

Use HCL syntax matching the file (2-space indent, `=` alignment within blocks as
the surrounding blocks do). Do NOT change the docker provider block, the
docker_socket variable, or any other resource.
  </action>
  <verify>
    <automated>grep -q 'variable "docker_group_id"' templates/python-ai/main.tf && grep -q 'group_add' templates/python-ai/main.tf && grep -q 'host_path *= *"/var/run/docker.sock"' templates/python-ai/main.tf && grep -q 'container_path *= *"/var/run/docker.sock"' templates/python-ai/main.tf && grep -q "stat -c '%g' /var/run/docker.sock" templates/python-ai/main.tf</automated>
  </verify>
  <done>
main.tf declares a `docker_group_id` string variable (default "999") documenting
the `stat -c '%g' /var/run/docker.sock` discovery; docker_container.workspace
mounts the host socket (host_path/container_path = /var/run/docker.sock,
read_only = false) and sets `group_add = [var.docker_group_id]`; the header
comment block mentions the mounted socket and docker/docker compose availability.
No other resource or the docker provider block changed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Static Terraform verification — fmt + validate</name>
  <files>templates/python-ai/main.tf, templates/python-ai/Dockerfile</files>
  <action>
Run static Terraform verification against templates/python-ai/ (per project
memory "infra-needs-live-deploy-gate": static checks are necessary but NOT
sufficient — the live deploy gate is surfaced separately, see below).

Steps (from templates/python-ai/):
  1. `terraform fmt` on the directory, then confirm no diff remains
     (`terraform fmt -check` exits 0). If fmt rewrote the file, that IS the fix —
     leave the reformatted file; re-run `-check` to confirm 0.
  2. `terraform init -backend=false` (required before validate; -backend=false
     avoids needing any backend/state for a syntax+provider check).
  3. `terraform validate` — must exit 0 with "Success! The configuration is valid."

If `terraform` is not installed in this environment, do NOT fake success: record
in the SUMMARY that static validation could NOT be run here and MUST be run
before push, and treat both `terraform fmt -check` and `terraform validate` as
part of the LIVE-VERIFY gate below.

LIVE-VERIFY gate (surface in SUMMARY — likely CANNOT complete in this session,
requires a full deploy; MUST NOT be silently skipped):
  - Push the template to the Coder server (`coder templates push` from
    templates/python-ai/) and create a workspace from it.
  - Inside the workspace confirm: `docker ps` succeeds (talks to host daemon),
    `docker compose version` reports the plugin, and a minimal
    `docker compose up` (e.g. a hello-world service) starts a SIBLING container.
  - Confirm the `docker_group_id` default ("999") actually matches the host:
    run `stat -c '%g' /var/run/docker.sock` on the host; if it differs, the
    operator MUST override `docker_group_id` (else socket access is
    permission-denied). Record the observed GID in the SUMMARY.
  - Note the two operator gotchas observed in practice: compose bind mounts
    resolve against the HOST fs; socket access = effective host root.
  </action>
  <verify>
    <automated>cd templates/python-ai && terraform fmt -check && terraform init -backend=false >/dev/null && terraform validate</automated>
  </verify>
  <done>
`terraform fmt -check` reports no diff and `terraform validate` returns
"Success! The configuration is valid." for templates/python-ai/. If terraform is
unavailable in this environment, the SUMMARY explicitly records that static
validation must run before push and folds fmt+validate into the LIVE-VERIFY gate.
The LIVE-VERIFY deploy gate (push + create workspace + `docker ps` /
`docker compose up` + host socket GID confirmation) is documented in the SUMMARY,
not marked done.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace container → host Docker daemon | The mounted `/var/run/docker.sock` lets any process in the workspace command the host daemon — effective root on the host. |
| compose file → host filesystem | Bind mounts declared in workspace compose files resolve against the HOST fs, not the workspace fs (path-confusion / unintended host access). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7c0-01 | Elevation of Privilege | mounted /var/run/docker.sock in workspace | high | accept | Documented, single-operator design (matches project "single-operator" posture in CLAUDE.md and templates/docker socket-GID note). Comment in Dockerfile + main.tf explicitly warns socket access = host root and is NOT for shared/multi-tenant use. |
| T-7c0-02 | Information Disclosure | compose bind mounts resolving against host fs | medium | accept | Dockerfile comment warns operators that compose bind-mount paths are HOST paths. No code mitigation possible for docker-outside-of-docker; documentation is the correct control. |
| T-7c0-03 | Tampering | apt install of docker-ce-cli + docker-compose-plugin from download.docker.com | high | mitigate | Install uses Docker's OFFICIAL apt repo over HTTPS with a pinned GPG keyring under /etc/apt/keyrings (signed-by), matching the established gh/PostgreSQL install pattern in this same Dockerfile. Packages are first-party Docker, Inc. — not third-party registry packages, so no npm/pip/cargo legitimacy gate applies. |
</threat_model>

<verification>
- Dockerfile: docker-ce-cli + docker-compose-plugin installed from
  download.docker.com with an /etc/apt/keyrings/docker.gpg keyring, arch-aware,
  before `USER coder`; no docker-ce engine / containerd installed; PostgreSQL
  DinD note intact.
- main.tf: docker_group_id variable (default "999") documenting
  `stat -c '%g' /var/run/docker.sock`; socket volume mount; group_add; header
  comment updated.
- `terraform fmt -check` clean and `terraform validate` Success for
  templates/python-ai/ (or explicitly deferred to LIVE-VERIFY if terraform is
  unavailable here).
- LIVE-VERIFY deploy gate documented in SUMMARY (not silently skipped).
</verification>

<success_criteria>
- python-ai workspace image ships `docker` (docker-ce-cli) + `docker compose`
  (docker-compose-plugin), no nested engine.
- The workspace container mounts the host socket and joins the host docker group
  via `group_add = [var.docker_group_id]`.
- Static Terraform checks pass (or are explicitly folded into the live gate).
- The deploy-time live-verify gate is surfaced for the operator, honoring the
  project memory that infra changes need a live deploy to trust.
</success_criteria>

<output>
Create `.planning/quick/260816-7c0-give-python-ai-template-docker-access-vi/260816-7c0-SUMMARY.md` when done.
The SUMMARY MUST include the LIVE-VERIFY deploy gate (push template, create
workspace, confirm `docker ps` + `docker compose up`, confirm host socket GID
matches docker_group_id) as an explicit outstanding item, not a silent skip.
</output>
